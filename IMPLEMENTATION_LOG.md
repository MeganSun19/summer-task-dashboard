# 实施日志

## 2026-08-09：长期课程在顺延学习日即时生效

- 修复长期课程只按学习日记录日期选择版本的问题；顺延中的旧学习日现在按实际使用日期应用已生效的新版本。
- 新版本启用数学、写字等模块时，孩子端会补齐任务卡片；同名任务的完成、免除状态继续保留。
- 首次发布的新版本默认从今天生效，预览文案明确说明当前顺延学习日的更新规则。
- 浏览器复现从 4 个模块发布同日 v2 恢复为 6 个模块，已完成的英语任务保持完成；完整自动测试 71 项通过。

## 2026-08-07：英语实验版合并回正式仓库

- 当前完整产品不再只是英语实验版，统一回到 `summer-task-dashboard` 维护。
- 保留原正式仓库 GitHub 历史和远程地址；旧正式提交标记为 `pre-english-island-release`。
- `summer-task-dashboard-english` 的完整工作树镜像进入正式仓库，验证和推送成功后删除重复目录。
- 浏览器存储键与线上 CloudBase 入口不因本地目录改名而改变。

这份文件用于跨 session 接续开发。记录的是已经落到文件中的事实，不记录聊天过程。

## 当前状态

- 当前阶段：独立英语实验版已具备本机音频导入、Track 05 听音辨词和双孩子独立进度，等待孩子试用。
- 当前可运行版本：本地英语实验版；使用独立 localStorage，暂不连接正式 Supabase，也尚未部署独立 CloudBase 环境。
- 数据事实来源：RAZ 使用 `../outputs/raz_bf_catalog/raz_b_to_f_catalog.xlsx`；古诗使用《2年级上 古诗诵读(1).pdf》；自然拼读教材使用 `../outputs/牛津树自然拼读1教材.pdf`。
- 当前阻塞：Oxford 教材音频是否允许重新托管尚未确认，因此音频只保存在家庭设备浏览器的 IndexedDB，不进入 CloudBase 发布包。
- 下一步：让哥哥和弟弟各完成一次 Track 05 试用，记录能否独立开始、依次选择和重听；再决定下一单元及专用实验后端。

## 2026-07-31 · 步骤 1：理解产品与检查原型

### 完成

- 阅读原 README、`index.html`、`styles.css` 和 `app.js`。
- 确认原型只有孩子/家长两个笼统视图，没有哥哥和弟弟独立数据。
- 确认数据只保存在浏览器 `localStorage`，还没有 Supabase。
- 确认原型已有每日任务生成、打卡、家长编辑和周概览的基础逻辑。

### 决策

- 核心目标定义为“减少家长干预”，而不是“增加打卡数据”。
- 双孩子设计以合作为主、轻竞争为辅，不做公开排名。
- 第一阶段先把孩子自主执行路径做通，再接云端和游戏。

## 2026-07-31 · 步骤 2：核对 RAZ Excel

### 检查文件

- `../outputs/raz_bf_catalog/raz_b_to_f_catalog.xlsx`

### 核对结果

- `Catalog`：470 本 B–F 级书目。
- `4-Week Plan`：28 条日计划，字段为 Week、Day、Focus、Books、Target Words、Sentence Frames、Parent Task。
- `Word-Book Map`：12 组目标词与书目映射。
- `Parent Guide`：7 条执行建议。
- `Missing`：3 条目录字数信息缺失，不影响 28 天日计划。

### 决策

- 28 天计划是 RAZ 每日任务的事实来源。
- 孩子端不直接显示“Parent Task”，而是把内容转成简短可执行步骤。
- 课程表内容目前沿用并增强原 `app.js` 内的 28 天数组；后续应迁移到独立数据或 Supabase。

## 2026-07-31 · 步骤 3：双孩子本地数据模型

### 完成

- 本地存储键升级为 `summer-task-dashboard-v1`。
- 新增 `brother`、`younger` 两个 profile。
- 两个孩子按日期分别保存任务、完成状态、错题和备注。
- 阳光由历史完成任务数计算，每项 10 阳光。
- 新增按连续全完成日计算的 streak。

### 文件

- `app.js`

### 已知限制

- v0 单孩子数据不自动迁移，以免错误归属给某一个孩子。
- 阳光暂时没有兑换和扣减流水。
- 数据仍只存在单一浏览器。

## 2026-07-31 · 步骤 4：孩子自主任务界面

### 完成

- 重构首页为“双星学习岛”。
- 新增哥哥/弟弟身份切换。
- 新增角色、进度、阳光、连续天数和预计时长。
- 每张任务卡同时显示任务内容和“怎么做”。
- RAZ 任务固定呈现听、读、找词、说一句的动作路径。
- 补充英语听力，使每日模块与产品需求一致。
- 每完成一项立即显示阳光反馈；全部完成显示庆祝卡片。
- 加入移动端响应式布局。

### 文件

- `index.html`
- `styles.css`
- `app.js`

## 2026-07-31 · 步骤 5：家长总览

### 完成

- 同屏显示哥哥、弟弟的完成数量、剩余任务和阳光。
- 支持选择日期。
- 支持选择孩子并编辑当天每项任务。
- 家长可以修改孩子实际看到的执行步骤。
- 支持记录错词、错题和明日注意事项。
- 支持恢复某个孩子当天的默认计划。

### 文件

- `index.html`
- `styles.css`
- `app.js`

## 验证记录

### 已通过

- `node --check app.js`：JavaScript 语法检查通过。
- 工作簿所有 sheet 已成功读取。
- `4-Week Plan` 已进行表格值检查和视觉渲染检查。

### 尚未执行

- 跨日期 RAZ 计划映射测试。
- “恢复默认计划”按钮的破坏性确认提示。
- Supabase 多设备同步测试。

## 2026-07-31 · 步骤 6：浏览器和移动端验证

### 完成

- 使用本地 HTTP 服务加载真实页面。
- 在默认桌面视口检查首页视觉层级。
- 在 390×844 视口检查手机布局。
- 测试后已取消测试打卡，浏览器状态恢复为 0/6、0 阳光。

### 验证

- 页面标题为“双星学习岛”，6 张每日任务卡全部渲染。
- 浏览器控制台错误：0。
- 哥哥完成 RAZ 后：1/6、10 阳光。
- 切换弟弟后：0/6、0 阳光，确认两人数据隔离。
- 家长总览同时显示哥哥 1/6、弟弟 0/6。
- 家长编辑器切换孩子成功，保存后出现“当天计划已保存”。
- 手机视口 `scrollWidth === innerWidth === 390`，无横向溢出。
- 手机视口浏览器错误：0。

### 决策 / 已知限制

- 手机端首屏能看到身份选择、今日状态和第一张任务卡，信息顺序符合孩子执行路径。
- 暂不加入复杂动画，先观察孩子是否能理解现有步骤。
- 105 分钟是所有模块合计，不代表必须连续完成；后续可增加上午/下午分组。

### 下一步

- 新增 Supabase schema、RLS 策略和前端数据适配层，未配置云端时继续使用 localStorage。

## 2026-07-31 · 步骤 7：角色头像替换

### 完成

- 哥哥头像替换为《如果历史是一群喵》的乌龙。
- 弟弟头像替换为哈小浪。
- 从原始乌龙四宫格生成独立的 `乌龙头像.png`，保留原图不变。
- 大头像、身份切换和家长总览统一使用同一角色图。

### 验证

- 乌龙头像成功加载：650×650。
- 哈小浪原图成功加载：1274×1700。
- 手机端角色图裁切正常，无图片加载错误。

### 文件

- `乌龙.png`：原图。
- `乌龙头像.png`：应用使用的方形裁切图。
- `哈小浪.png`：应用使用的弟弟角色图。
- `app.js`、`styles.css`。

## 2026-07-31 · 步骤 8：阳光奖励闭环

### 完成

- 新增孩子奖励中心，展示可用阳光、奖励价格和兑换按钮。
- 默认奖励：选择故事、动画时间、家庭游戏、小惊喜。
- 新增兑换申请记录，状态为等待确认、兑换成功或已退回。
- 申请时冻结阳光，避免同一余额重复兑换。
- 家长可以同意或退回；退回自动返还阳光。
- 家长可以添加自定义奖励和设置阳光价格。
- 旧的 v1 本地状态会自动补齐奖励字段，不需要清空数据。

### 数据规则

- 累计获得阳光 = 历史完成任务数量 × 10。
- 已使用阳光 = 等待确认和已批准兑换的价格总和。
- 可用阳光 = 累计获得 − 已使用；已退回申请不扣除。

### 验证

- 初始 0 阳光时所有兑换按钮正确禁用。
- 完成 3 项后显示 30 可用阳光，30 阳光奖励可以申请。
- 申请后余额变为 0，孩子端显示“等待家长确认”。
- 家长端正确出现哥哥的 30 阳光申请。
- 家长退回后队列清空，余额恢复为 30，孩子端显示“已退回阳光”。
- 测试结束后完成状态恢复为 0/6、0 阳光。
- 390×844 手机视口无横向溢出，控制台错误为 0。

### 已知限制

- 自定义奖励目前只能新增，尚不能编辑、停用或删除。
- 已处理兑换记录没有独立的家长历史列表，只在孩子端显示最近 3 条。
- Supabase 接入前，奖励和兑换仍只保存在当前浏览器。

### 下一步

- 新增 Supabase schema、RLS 策略和数据适配层，将任务、阳光及兑换记录同步到多设备。

## 2026-07-31 · 步骤 9：Supabase 云端同步适配

### 完成

- 写入真实项目的 Project URL 和 publishable key；未使用任何高权限密钥。
- 新增 `supabase-schema.sql`：家庭、设备成员、家庭状态、RLS、RPC 和 Realtime publication。
- 家长 PIN 使用 `pgcrypto` 哈希保存，不存储明文。
- 新增家庭创建和邀请码加入流程；第一台设备创建时会上传现有本地数据。
- 新增 `revision` 乐观并发控制，其他设备先写入时不静默覆盖。
- 新增 Realtime 订阅，收到其他设备更新后刷新页面状态。
- 云端未初始化、网络失败或匿名登录未开启时继续使用 localStorage。
- `activeKid` 保持为设备本地偏好，不在设备之间同步。

### 文件

- `supabase-schema.sql`
- `supabase-config.js`
- `cloud-store.js`
- `index.html`
- `styles.css`
- `app.js`
- `README.md`

### 验证

- `node --check app.js`：通过。
- `node --check cloud-store.js`：通过。
- `node --check supabase-config.js`：通过。
- 桌面浏览器加载：6 张任务卡正常渲染。
- Supabase 返回 `Anonymous sign-ins are disabled` 时，顶部状态正确回退为“本地模式”。
- 本地模式完成 RAZ 后为 1/6、10 阳光，取消后恢复 0/6。
- 点击云端状态可看到“请先启用 Supabase 匿名登录”的明确提示。
- 390×844 视口：`scrollWidth === innerWidth === 390`，无横向溢出。
- 用户已在真实 Supabase 项目执行初始化 SQL，并启用 Anonymous Sign-Ins。
- 再次加载本地应用：匿名登录和 `get_my_families` RPC 成功，顶部显示“等待启用云端”，创建/加入家庭面板自动展开。
- 真实连接验证期间浏览器 error/warn 日志为 0，6 张任务卡继续正常渲染。

### 决策 / 已知限制

- 第一阶段以受 RLS 保护的 JSON 家庭状态作为迁移桥，先稳定多设备同步，再拆分细粒度业务表。
- 家长 PIN 目前用于新设备加入；家长中心内的敏感操作尚未逐项要求 PIN。
- SQL、匿名登录和无家庭状态查询已在真实项目验证；创建家庭、RLS 和双设备 Realtime 仍需端到端验证。

### 下一步

- 在 Supabase Dashboard 执行 `supabase-schema.sql`，启用 Anonymous Sign-Ins，再完成双设备端到端验证。

## 2026-07-31 · 步骤 10：CloudBase 免费静态部署

### 完成

- 创建并确认 CloudBase 免费体验环境 `twin-start-island-d7dxg7911b0684`，地域为上海。
- 使用 CloudBase CLI 3.7.0 登录并部署静态网站。
- 只上传运行所需的 7 个文件，发布包约 940KB；未上传 `.git`、SQL、README 和未使用的原始图片。
- 新增 `deploy-cloudbase.sh`，后续可重复生成纯净发布包并部署。
- 线上地址：`https://twin-start-island-d7dxg7911b0684-1461490716.tcloudbaseapp.com/`。

### 验证

- CloudBase CLI：7 个文件全部上传成功。
- 线上页面标题为“双星学习岛”，6 张任务卡正常渲染。
- 乌龙与哈小浪头像均加载成功。
- Supabase 初始化成功，无家庭成员时正确显示“等待启用云端”。
- 任务打卡验证：0/6、0 阳光 → 1/6、10 阳光；验证后恢复为 0/6、0 阳光。
- 390×844 手机视口：`scrollWidth === innerWidth === 390`，无横向溢出。
- 浏览器控制台 error/warn 日志为 0。

### 决策 / 已知限制

- 第一阶段只迁移静态托管，继续使用 Supabase 作为数据同步后端，避免同时重写身份、RLS 和 Realtime。
- 默认 CloudBase 域名用于开发和家庭试用，存在平台提示的访问频率及稳定性限制；稳定使用后再评估自定义域名。
- 免费环境到期时间为 2027-01-31，需要在到期前手动续期。

### 下一步

- 在 CloudBase 线上地址上用第一台正式设备创建家庭空间，记下邀请码，再用第二台设备加入并完成实时同步验证。

## 2026-07-31 · 步骤 11：手机云同步诊断版本

### 完成

- 云端初始化增加阶段标记：加载组件、读取登录状态、匿名登录、读取家庭成员关系、载入家庭数据。
- 未识别错误不再统一显示“云端暂不可用”，改为显示安全截断后的错误码、HTTP 状态和错误信息。
- 初始化失败时自动展开云端设置面板，便于手机直接复制或截图诊断内容。
- 为本地脚本 URL 增加 `20260731-1` 版本参数，避免手机继续使用旧缓存。
- 诊断信息会明确提示本地数据仍被保留，不改变家庭数据结构或同步规则。

### 验证

- `node --check app.js`、`cloud-store.js`、`supabase-config.js`：通过。
- `git diff --check`：通过。
- 模拟 Supabase 客户端缺失：正确显示阶段、`CLIENT_NOT_READY` 和本地数据保留提示。
- CloudBase CLI：7 个文件全部上传成功。
- 线上 HTML 已引用 `20260731-1`；线上 `cloud-store.js` 已包含阶段诊断逻辑。

### 决策 / 已知限制

- 错误信息会移除 URL、压缩空白并限制长度，避免在家庭页面展示过多底层细节。
- 实际故障码只能由出现问题的第二台手机触发；正常设备无法复现该手机的认证或网络环境。

### 下一步

- 在第二台手机强制刷新线上页面，记录自动展开面板中的完整诊断文字，再针对具体错误处理。

## 2026-07-31 · 步骤 12：修复第二台设备加入家庭的 42702

### 完成

- 诊断版本在第二台手机捕获到 PostgreSQL `42702`：`column reference "family_id" is ambiguous`。
- 根因定位到 `join_family` 的 `on conflict (family_id, user_id)`；`RETURNS TABLE` 同时声明了 `family_id` 输出变量。
- 改为 `on conflict on constraint family_members_pkey do nothing`，避免列名与输出变量冲突。
- 新增可独立执行的数据库补丁 `supabase-fix-join-family-42702.sql`，同时更新完整初始化 schema。

### 验证

- 检查其余 RPC 的同名输出变量和未限定列引用，未发现同类冲突。
- `git diff --check`：通过。
- 补丁只替换 `join_family` 函数及其执行权限，不修改或删除现有家庭数据。

### 决策 / 已知限制

- SQL 修复必须在 Supabase SQL Editor 执行后才会作用于线上数据库；静态站点无需再次部署。
- 失败的 RPC 调用已由 PostgreSQL 事务回滚，第二台设备可以在补丁执行后直接重试。

### 下一步

- 在 Supabase SQL Editor 执行 `supabase-fix-join-family-42702.sql`，然后在第二台手机重新加入家庭。

## 2026-07-31 · 步骤 13：真实双设备同步验收

### 完成

- 第一台手机创建家庭空间并上传初始数据。
- 第二台手机在修复 `42702` 后使用邀请码和家长 PIN 成功加入。
- 用户确认两台设备之间的数据可以同步。
- 确认微信内置浏览器与系统浏览器属于独立使用环境，各自只需首次加入一次；日常使用无需重复输入 PIN。

### 验证

- 第二台设备状态显示已加入家庭空间。
- 双设备数据变更同步成功。

### 决策 / 已知限制

- 家庭 PIN 只用于授权新的浏览器或 WebView 加入；清除网站数据、重装微信或换手机后需要重新加入。
- 匿名设备身份和家庭成员关系继续由 Supabase Auth、RLS 与本地持久会话维护。

### 下一步

- 提交并推送已验证的部署、诊断和 SQL 修复，然后完善奖励功能。

## 2026-07-31 · 步骤 14：累计阳光、植物商店与僵尸守卫战

### 完成

- 移除孩子申请、阳光冻结、家长审核和兑换扣除界面。
- 阳光改为纯累计：每完成一项任务增加 10，任何植物操作都不扣除。
- 新增 6 种植物，按累计阳光门槛永久解锁；每个孩子有独立的 5 格出战小队。
- 新增今日花园守卫战，任务完成数对应击退波数，全部完成后显示守卫成功。
- 旧状态继续兼容；历史 `rewards` 和 `redemptions` 字段不再参与余额计算。

### 验证

- `node --check app.js` 与 `git diff --check`：通过。
- 浏览器交互：完成 1 项后阳光从 0 增至 10、向日葵解锁、战斗进度变为 1/6。
- 种下向日葵后阳光仍为 10；完成 6 项后显示 60 阳光和“守卫成功”。
- 390×844 手机视口：页面宽度 390、无横向溢出、商店为两列布局。
- 浏览器控制台错误：0；测试任务及植物状态已恢复。

### 决策 / 已知限制

- 玩法只提供正反馈，没有失败惩罚、僵尸扣分或跨日追赶机制。
- 植物当前以 emoji 呈现，后续可替换为原创角色图和动画。

### 下一步

- 部署静态文件并在两台手机上确认新字段能够通过现有 Supabase JSON 状态同步。

## 2026-07-31 · 步骤 15：双星真实世界地图

### 完成

- 新增双人共享世界地图：同一天哥哥和弟弟都完成全部任务后才计为 1 个共同完成日。
- 每个共同完成日点亮 1 个国家；地图包含 195 个国家，不设置短期通关天数。
- 点亮路线从中国出发，依次向蒙古、朝鲜半岛、日本、中亚、南亚、东南亚等相邻或邻近区域扩张，再延伸到整个世界。
- 新增最近 5 站扩张足迹与下一站预告；当前国家使用橙色、近期区域使用亮黄色，形成由点到线到面的视觉变化。
- 真实国家边界使用 Wikimedia Commons `BlankMap-World.svg`（Public domain），静态资源随站点本地部署。
- 部署脚本已增加 `world-map.svg`，发布文件数由 7 个变为 8 个。

### 验证

- 195 个国家代码全部唯一，且每个代码都能匹配 SVG 中的国家边界。
- 只完成哥哥当天任务：地图保持 `0/195`；弟弟也完成后：中国变为橙色，进度变为 `1/195`。
- 下一站显示蒙古，最近足迹显示“中国 → 蒙古”。
- 390×844 手机视口：真实地图完整显示，`scrollWidth === innerWidth === 390`，无横向溢出。
- 浏览器控制台错误为 0；测试结束后两位孩子均恢复 `0/6`，地图恢复 `0/195`。

### 决策 / 已知限制

- 扩张顺序以地理连续性和视觉连片为目标，海岛国家使用邻近海域关系，不代表严格的陆地接壤图。
- 微型岛国计入 195 国进度，但在小比例尺手机地图上轮廓可能很小，名称通过最近足迹和下一站显示。

### 下一步

- 继续完善双人学习岛的共享互动，再统一部署并进行双设备同步验收。

## 2026-07-31 · 步骤 16：双人联合防线与可恢复补做闭环

### 完成

- 将个人 6 波守卫战升级为哥哥、弟弟共用的 12 波联合防线，进度直接从两人的既有 `task.done` 汇总。
- 花园同时展示哥哥与弟弟两支 5 格植物小队；当前角色可编辑自己的小队，另一支小队保持可见。
- 在共同完成 3、6、9、12 项时依次触发“双星鼓舞、藤蔓联结、双星连击、守护结界”，并按两人状态显示乌龙与哈小浪互动。
- 家长历史日期编辑器新增“已完成（可用于登记补做）”；过去某天两人后来都完成后，世界地图会自动补点亮。
- 不新增失败、扣分或一次性点亮字段；联合进度、技能和地图均由既有家庭 JSON 状态派生，因此无需 Supabase schema 迁移或重建家庭空间。
- 更新静态资源版本号，避免部署后浏览器继续使用旧版 CSS/JS。
- 已通过 `deploy-cloudbase.sh` 将 8 个静态文件一次性部署到现有 CloudBase 环境。

### 验证

- `node --check app.js` 与 `git diff --check`：通过。
- 双人状态：哥哥完成 6 项时显示 `6/12` 并触发前两档技能；弟弟完成 3 项后显示 `9/12`；两人完成后显示联合守卫成功与全部四档技能。
- 两支小队：哥哥、弟弟各加入向日葵、豌豆射手和坚果墙后，同一页面同时显示两队，各 5 个槽位。
- 历史补做：过去日期先登记哥哥 `6/6` 时地图不增加；再登记弟弟 `6/6` 后共同完成日与国家数自动增加 1。
- 390×844：页面宽度与滚动宽度均为 390，两队纵向排列，无横向溢出；1280px 桌面端两队并排。
- 浏览器控制台错误：0；测试任务、植物与历史补做状态均已恢复。
- 线上首页、CSS、JS、云同步脚本、两张头像与 `world-map.svg` 全部返回 HTTP 200；此前未部署的地图资源已从 404 恢复为 200。
- 线上 1280px 桌面端：联合防线、四个技能、两队双栏和 319 个地图路径正常渲染，无横向溢出。
- 线上 390×844 手机端：`innerWidth === scrollWidth === 390`，两队单栏、地图和技能栏正常渲染，控制台错误为 0。

### 决策 / 已知限制

- 双人联合状态继续保存在现有 `family_states.state` JSON 中，只复用 `days` 与 `gardens`；不在本轮拆表。
- 家长登记历史补做会像普通完成一样增加该孩子累计阳光，符合“补做可恢复”原则。

### 下一步

- 在家庭日常使用中观察两台真实设备的实时同步体验；若出现具体冲突，再针对该场景补充诊断，不需要重建家庭空间。

## 2026-07-31 · 步骤 17：地图 404 缓存恢复

### 完成

- 地图请求改为带版本号的 `world-map.svg?v=20260731-1`，并使用 `cache: "no-cache"` 重新校验资源。
- 应用脚本版本更新为 `app.js?v=20260731-5`，避免部署前缓存的地图 404 继续触发失败提示。

### 验证

- 线上 `world-map.svg` 当前返回 HTTP 200、`image/svg+xml`；此前失败响应允许缓存 300 秒，符合用户仍看到旧错误的现象。
- 缓存修复已重新部署；带版本号的首页加载 `app.js?v=20260731-5`，脚本请求 `world-map.svg?v=20260731-1`，地图返回 HTTP 200、`image/svg+xml`、1,101,801 字节。

### 下一步

- 用户通过带版本号的首页地址刷新一次，确认原设备不再显示旧的地图失败缓存。

## 2026-07-31 · 步骤 18：移动 WebView 地图兼容模式

### 完成

- 可交互 SVG 仍为首选；如果 `fetch`、文本解析或 DOM 导入任一步失败，自动改用浏览器原生 `<img>` 渲染同一地图资源。
- 兼容模式保留共同完成日、点亮国家数、最近足迹和下一站信息，不再显示空白地图或要求反复刷新。
- 地图、CSS 和应用脚本版本号再次更新，确保问题设备获取本轮代码。

### 下一步

- 部署后由出现问题的原设备打开带新版本号的首页，确认至少能显示兼容模式地图。

## 2026-07-31 · 步骤 19：家长多层级任务计划

### 完成

- 家长中心新增“总体任务”，可分别为哥哥、弟弟控制长期默认项目，并修改默认名称和执行步骤；RAZ 书目、古诗和练习内容继续按日期自动变化。
- 保留“当天任务”用于单日精细修改，保存后明确标记为单日安排，不会被总体任务自动覆盖。
- 新增“日期区间计划”，可选择哥哥、弟弟或两人，并设置开始、结束日期；区间外自动恢复总体任务。
- 新增“手部休养”预设，将写字练习和书面数学替换为中文朗读复述、英语口语练习，其他日期课程内容继续正常轮换。
- 区间计划可取消并恢复总体任务；已有同名任务的完成状态、错题和家长备注会保留。
- 总体任务数量变化后，今日进度、阳光、联合防线、技能阈值和世界地图完成判断均按实际任务数计算。
- 新字段 `taskSettings` 和 `planPeriods` 保存在原有家庭 JSON 状态中，旧家庭数据会自动补齐，无需 Supabase schema 迁移。
- 已通过 `deploy-cloudbase.sh` 将 8 个静态文件部署到现有 CloudBase 环境。

### 验证

- `node --check app.js` 与 `git diff --check`：通过。
- 日期区间：哥哥 2026-08-01 至 2026-08-10 显示 6 项无书写任务；弟弟不受影响；2026-08-11 自动恢复普通任务。
- 刷新页面后区间安排仍然存在；取消区间后恢复总体任务。
- 总体任务：关闭弟弟的写字项目后显示 5 项，联合防线自动从 12 波调整为 11 波；恢复总体任务后回到 6 项。
- 当天任务：修改单日标题并保存成功，恢复当天默认任务后正确还原。
- 总体任务保存未修改字段后，下一日期的 RAZ 操作步骤仍随当天课程变化，没有被冻结为前一天内容。
- 390×844 手机视口：`innerWidth === scrollWidth === 390`，总体任务与日期区间面板单栏显示，无横向溢出。
- 浏览器控制台 error/warn：0。
- CloudBase CLI：8 个文件全部上传成功；线上 `index.html`、`app.js`、`styles.css` 与本地 SHA-256 完全一致，并引用 `20260731-7` 版本资源。

### 下一步

- 在家庭线上空间选择受伤的孩子，并应用 2026-08-01 至 2026-08-10 的“手部休养”计划；另一台手机刷新后确认区间安排同步。

## 2026-08-03 · 步骤 20：8 月学习计划调整

### 完成

- 依据《2年级上 古诗诵读(1).pdf》逐页核对 18 周必背、选背内容，并内置到古诗任务；习字内容未纳入。
- 以 8 月 3 日复习第 9 周为锚点，按“新学一周、次日复习”推进，8 月 22 日在第 18 周复习完成后回到第 1 周。
- 8 月 3–9 日两人统一为英语动画 30 分钟、阅读 30 分钟；哥哥增加乘法口诀练习册 2 页，弟弟无书写任务。
- 8 月 10–31 日只保留古诗任务，其他项目标记为待重新规划，不改变长期总体任务。
- 新增计划版本迁移，覆盖旧区间计划生成的冲突说明，同时保留完成状态、错题以及家长明确保存的单日备注。

### 验证

- `node --check app.js`、`git diff --check`：通过。
- 页面核对：8 月 3 日第 9 周复习；8 月 4 日第 10 周学习；8 月 5 日第 10 周复习；8 月 21 日第 18 周复习；8 月 22 日第 1 周学习；8 月 31 日第 5 周复习。
- 8 月 3–9 日哥哥 4 项、约 85 分钟，弟弟 3 项、约 70 分钟；8 月 10 日起两人均为 1 项古诗。
- 浏览器控制台错误：0；任务卡片长文本排版正常。
- CloudBase：8 个静态文件发布成功；线上 `index.html`、`app.js`、`styles.css` 与本地 SHA-256 完全一致，并引用 `20260803-1` 版本资源。

### 下一步

- 8 月 10 日前根据恢复情况重新确定英语、阅读和数学安排，再新增下一段明确日期计划。

## 2026-08-03 · 步骤 21：英语自主学习模块方案检查点

### 已完成的分析

- 核对 `../outputs/raz_bf_catalog/raz_b_to_f_catalog.xlsx`：`4-Week Plan` 共 28 天，其中 23 天包含目标词；合计 194 次目标词安排、138 个不同单词。
- 确认这 138 个词并非全部是高频词，实际混合了高频功能词、可拼读词、主题词和阅读理解词。因此原计划应定位为“RAZ 词汇与阅读迁移课程”，不能直接作为系统拼写课。
- 核对当前孩子端：英语 RAZ 仍是文字任务卡，只有说明、标签和完成按钮；尚无音频播放、听音选词、字母拼词、自动判定和错词回流。
- 讨论并确定英语模块应以减少家长逐项介入为首要目标，家长只保留每周一次的短时抽查。

### 已确认的产品决策

- 课程与数据层分成三条独立进度线：自然拼读、高频/心词、RAZ 阅读迁移。
- 孩子端不显示三个割裂入口，而是合并成一次“英语岛探险”：声音实验室 → 心词锻造屋 → RAZ 故事森林。
- 每个 RAZ 目标词需要标记为 `decodable`、`heart`、`vocabulary` 或 `future`；自然拼读进度决定何时把 `future` 转为可拼读练习。
- 每天建议自动组合：1 个拼读规律、4–6 个规则词、1 个新心词、3 个旧词、1 本 RAZ 锚点书及一段听力。
- 错词按第 1、3、7、14 天自动回流；两个孩子分别保存练习次数、正确率和掌握状态。

### 音频技术决策

- 单独音素不使用普通浏览器 TTS；第一阶段采用随静态网站部署的统一口音 MP3 音素包。
- 普通单词、句子和操作提示可暂时使用浏览器 `speechSynthesis`。
- 不建议孩子每次点击时实时调用云端 TTS：静态前端不能暴露云服务密钥，且会增加云函数、缓存、失败重试和网络延迟。
- 如需机器生成音素，优先在开发阶段用支持 IPA/SSML 的云端 TTS 一次性生成，再人工验音并保存为静态文件；预计无需持续付费。
- 第一版可只制作 8 月用到的约 10 个重点元音，再逐步补齐约 40–44 个英语音素。

### 尚未实施 / 待确认

- 本步骤只完成分析和产品决策；尚未修改英语任务代码、工作簿或云端数据结构，也尚未加入任何音频资源。
- 开发前需确认实际教材是否为 `Oxford Phonics World 1–3`、采用美音还是英音，以及可合法使用的教材音频或页面资源。
- 网页内字母拼词可以自动判定，但纸笔手写无法可靠自动检查；建议保留每周一次约 10 分钟的家长听写抽查。

### 下一步

- 先定义英语课程数据结构和 8 月每日生成规则，再实现“声音实验室”的最小可用版：静态音素播放、听音选择、字母块拼词、自动判定和独立进度保存。

## 2026-08-03 · 步骤 22：Oxford Phonics World 官方音频检查

### 检查结果

- 检查目录 `/Users/megsun/Documents/P-files/kids/oxford-phonics-world-level1-disc1`：包含 `OPW_SB1_Disc1_Track01.mp3` 至 `Track60.mp3` 共 60 条 MP3，总计约 53MB。
- 单条音轨约 18 秒至 153 秒，是整段教材课程/练习音轨，不是“一音一文件”的独立音素库。
- 技术上浏览器可直接播放这些 MP3，也可建立“册—单元—页码—轨道”映射；但若用于单独音素练习，还需人工标注音素在轨道中的时间点或另行准备短音频。

### 决策 / 版权约束

- 用户已明确资源仅供自己家庭内部学习使用；后续技术方案按家庭专用资源设计，不面向公众提供课程或商业服务。
- 这些音频可用于本机开发和家庭内部验证，但未确认 Oxford ELT 的具体再发布许可前，不复制到 CloudBase 静态发布包。
- CloudBase 中的所谓“本地音频”仍会作为线上文件提供下载；仅凭链接不公开并不等于私人本地使用。
- 如果官方条款允许家庭或授权用户使用，可考虑放入需要家庭身份认证的私有存储并通过短时链接播放；如果不允许重新托管，则只把教材音频作为外部/本地参考，应用自己的音素包仍使用真人录制或一次性 TTS 生成。

### 下一步

- 建立“册—单元—教材页—原始轨道—片段起止时间—资源类型—目标音/词”清单；先用 Level 1 做一单元样例，经人工试听确认后再批量切片。上线前再决定使用公开静态文件还是家庭认证后的私有存储。

## 2026-08-03 · 步骤 23：Oxford Phonics World Level 1 Unit 1 映射样例

### 完成

- 读取 `../outputs/牛津树自然拼读1教材.pdf`，确认文件共 107 页；Unit 1 位于 PDF 第 5–12 页、书页 4–11，Unit 2 从 PDF 第 13 页开始。
- 逐页视觉核对 Unit 1：Aa、Bb、Cc，教材标注 Disc 1 Track 03–14。
- 新增 `curriculum/opw-level1-unit1-audio-map.json`，记录册、单元、PDF 页、书页、练习、原始音轨、完整片段起止时间、候选尾部切点、资源类型、目标音、目标词和人工试听状态。
- 新增 `scripts/validate-opw-audio-map.mjs`，检查 Track 03–14 是否齐全、本地 MP3 是否存在、文件时长与映射是否一致、片段范围是否合法。
- 教材音频仍只引用原目录，未复制进项目，也未加入 CloudBase 部署包。

### 验证

- PDF 视觉检查：Unit 1 八页排版、页码、练习字母及 Track 03–14 均与映射一致。
- `ffprobe`：12 条音轨实际时长与映射误差均不超过 0.02 秒。
- `ffmpeg silencedetect`：生成每条音轨的候选尾部切点；这些切点尚未视为人工验音结论。
- `node scripts/validate-opw-audio-map.mjs`：通过。

### 决策 / 已知限制

- 当前 `clipStartSeconds` 至 `clipEndSeconds` 表示未经裁剪的完整原始音轨；`candidateTrimEndSeconds` 只是根据尾部静音计算的候选值。
- Track 05 是 6 个纯音频听辨提示，教材页面不显示所读单词，必须人工试听后补录，不能仅凭页面推断。
- 尚未生成或提交任何音频切片；教材资源继续遵循“仅家庭内部、本地引用、不得发布”的约束。

### 下一步

- 人工试听 Track 03–14，确认各候选尾部切点和 Track 05 的 6 个词；将 `listeningReview` 改为 `verified` 后，再用该映射实现声音实验室的播放与听音选择样例。

## 2026-08-03 · 步骤 24：Track 05 人工听辨转录

### 完成

- 用户人工试听 Track 05，并按顺序确认六个听辨词：`ant`、`bear`、`apple`、`alligator`、`cup`、`ax`。
- 将用户输入的 `aligator` 规范为教材标准拼写 `alligator`。
- 更新 Unit 1 音频映射，将 Track 05 的 `listeningReview` 单独标记为 `verified`；其他音轨没有人工试听证据，继续保持 `required`。

### 验证

- `node scripts/validate-opw-audio-map.mjs`：通过。
- `jq empty curriculum/opw-level1-unit1-audio-map.json`：通过。

### 决策 / 已知限制

- 声音实验室首个样例可使用完整 Track 05 和六词顺序，不需要等待所有候选裁剪点确认。
- 其他音轨在未人工试听前不标记为已确认；若后续用于逐词播放，仍需单独标注词级时间点。

### 下一步

- 实现 Track 05 的本地“听音辨词”样例：播放完整音轨，依次选择六个词并自动判定；音频继续只作本地引用，不进入公开部署包。

## 2026-08-03 · 步骤 25：Track 05 本地听音辨词原型

### 完成

- 新增 `prototypes/sound-lab-track05.html`，通过本机文件选择器载入 Track 05；音频仅在当前浏览器生成临时对象 URL，不复制、不上传。
- 页面按已确认顺序自动判定 `ant → bear → apple → alligator → cup → ax`，错误答案停留在当前题并提示重听，正确答案进入下一题。
- 新增完成进度、答案记录、完成反馈和重新开始功能。
- 新增 `prototypes/README.md`，记录本地使用方式和“不进入 CloudBase 发布包”的边界。

### 验证

- 浏览器载入真实 `OPW_SB1_Disc1_Track05.mp3`：文件名及 50 秒音频控件正常显示。
- 错误选择 `apple` 作为第一题：显示“再听一下”，进度不前进。
- 按六词正确顺序选择：进度依次更新并最终显示“完成 6/6”。
- 390×844 手机视口：`innerWidth === scrollWidth === 390`，无横向溢出。
- 浏览器控制台 error/warn：0。
- `node scripts/validate-opw-audio-map.mjs` 与 `git diff --check`：通过。

### 决策 / 已知限制

- 本原型以完整 Track 05 连续播放为前提，没有逐词回放；逐词播放仍需要人工标注六个词的词级起止时间。
- 浏览器不会记住上次选择的本地 MP3，重新打开页面后需要再次选择文件。
- 原型与正式孩子端隔离，未加入 CloudBase 部署脚本，避免公开托管教材音频。

### 下一步

- 让孩子完成一次不提示的试用，记录是否能自主载入音频、跟随六个提示选择单词；根据结果决定正式版的音频授权与存储方式。

## 2026-08-03 · 步骤 26：独立英语实验版试用闭环

### 完成

- 建立独立目录 `summer-task-dashboard-english`；部署脚本必须显式传入实验环境 ID，并拒绝正式环境 ID。
- 家长端支持导入 Oxford Disc ZIP，检查册、光盘、连续音轨后解压到本机 IndexedDB；教材 MP3 不复制进项目，也不上传 Supabase 或 CloudBase。
- 正式孩子端接入 Track 05 声音实验室，不再需要孩子每次选择 MP3。
- Track 05 正确答案为 `ant → bear → apple → alligator → cup → ax`，支持即时判定、答题进度和重新练习。
- 声音实验室按孩子和日期分别保存答对数量、重听次数、完成时间和更新时间；刷新后可继续。
- 实验版使用 `summer-task-dashboard-english-v1` 和独立家庭键；正式 Supabase 配置已移除，避免试用污染正式家庭数据。
- 修复云端未配置时自动展开设置面板的问题；实验版默认安静保持本地模式。

### 验证

- `node --check app.js audio-store.js audio-library-ui.js cloud-store.js supabase-config.js`：分别通过。
- `node scripts/validate-opw-audio-map.mjs`：通过，Unit 1 共 12 条映射。
- `sh -n deploy-cloudbase.sh`：通过。
- 浏览器导入真实 Track 01–05 测试 ZIP：识别为 Level 1 Disc 1、5 条、3.6 MB，Track 05 可播放并开始答题。
- 哥哥做到 2/6 后切换弟弟：弟弟为 0/6；切回哥哥及刷新页面后仍为 2/6。
- 390×844：`innerWidth === scrollWidth === 390`，无横向溢出。
- 修复后新开页面控制台 error/warn：0。

### 决策 / 已知限制

- 当前只是家庭设备上的试用版，尚未创建或部署独立 CloudBase 环境。
- 只实现 Unit 1 Track 05；尚未接入完整自然拼读课程、心词、RAZ 迁移和错词间隔复习。
- 浏览器可能拒绝持久存储申请，家长必须保留原始 ZIP 备份。
- 自动化验证不能替代儿童可用性观察，下一步仍需孩子本人完成一次无提示试用。

### 下一步

- 哥哥和弟弟各进行一次无提示试用，分别记录是否能独立找到入口、开始播放、理解六词顺序、处理错误反馈和完成练习。

## 2026-08-03 · 步骤 27：家庭儿童最小 MVP 验证

### 完成

- 孩子已实际试用英语实验版，家庭反馈为“基础功能 OK”。
- 据此确认 Track 05 的本机音频、开始练习、听音选择、即时反馈和进度保存构成的最小闭环没有阻断问题。
- 最小 MVP 的可用性假设验证通过；该结论只覆盖基础闭环，不代表 Oxford 1–3 完整课程、长期留存或学习效果已经验证。

### 决策 / 已知限制

- 下一阶段回到既定课程主线：先结构化 Oxford Phonics World 1–3 的教材、单元、音轨和必要片段，再制定自然拼读、心词、RAZ 三线总体计划及每日任务。
- 音频“拆解”优先保存原始音轨并建立结构化时间点，只生成实际练习需要的片段，避免无目的地批量复制教材音频。
- 继续保持家庭本地资源边界，不把教材 MP3 加入公开部署包。

### 下一步

- 盘点 Oxford 1–3 的教材 PDF、Disc 音频完整性及现有映射，定义三册统一的课程音频数据结构。

## 2026-08-03 · 步骤 28：Oxford 1–3 教材与 Disc 1 统一映射

### 完成

- 新增 `curriculum/opw-disc1-catalog.json`，统一记录三册教材的 Unit/Review、PDF 页、书页、学习重点、音频覆盖状态和逐轨映射。
- 三册 Disc 1 的 60、65、68 个文件全部入目录；Track 01–02 保留为前置/补充音轨，Track 03 起映射到教材活动页。
- 确认三册 Disc 1 均只覆盖 Unit 1–4 与 Review 1–2；Unit 5–8 和 Review 3–4 属于当前未提供的 Disc 2，已显式标为不可用。
- 每条音轨保留原始文件，登记完整可播区间和尾部静音候选点；没有批量物理切片。
- 已知的 Level 1 Track 05 六词顺序继续保留，但未伪造逐词时间点；其他子片段统一标为使用前需人工试听。
- 新增 `scripts/build-opw-disc1-catalog.mjs` 和 `scripts/validate-opw-disc1-catalog.mjs`，可从真实 MP3 重建目录并验证文件连续性、页码归属和时长。

### 验证

- 逐册检查目录页和 PDF 5–84 页 contact sheet，并对 Unit/Review 边界及多音轨页面做原图抽查。
- `node scripts/build-opw-disc1-catalog.mjs`：生成 Level 1 60 条、Level 2 65 条、Level 3 68 条记录。
- `node scripts/validate-opw-disc1-catalog.mjs`：三册均通过连续音轨、真实文件、时长、片段范围和 section 页码校验。
- 尾部静音检测：193 条音轨均得到候选终点；候选裁剪只作为元数据，不作为已人工确认的切片。

### 决策 / 已知限制

- 课程生成器只能使用 `audioAvailability: available-disc1` 的 section；不能因为教材页存在就安排 Unit 5–8 的音频任务。
- 默认可用粒度是“完整教材活动音轨”。只有具体练习确实需要重复单词/提示时，才新增子片段时间点。
- Track 01 以及 Level 2–3 Track 02 尚未建立教材活动归属，保持可追踪但不可自动排课。
- 教材音频继续只作家庭本地引用，`*.mp3` 与 `*.zip` 不进入 Git 或部署包。

### 下一步

- 基于当前可用范围制定三条课程线总体计划：自然拼读为主轴，心词与 RAZ 作为同步迁移和复习配套。

## 2026-08-03 · 步骤 29：英语实验版 Git version2 基线

### 完成

- 将英语实验版作为独立 Git 仓库管理，当前完整状态提交为 version2 基线。
- `.gitignore` 增加 `tmp/`、`*.mp3` 和 `*.zip`，降低家庭教材音频或测试包被误提交的风险。

### 决策 / 已知限制

- 英语实验版与正式版继续分仓，避免实验课程和本地数据逻辑污染正式孩子端。
- `version2` 用于回滚当前课程结构化基线；后续课程计划和生成器按小步提交演进。

### 下一步

- 制定自然拼读、心词、RAZ 三条课程线的总体计划并作为下一独立版本提交。

## 2026-08-03 · 步骤 30：Oxford 1-3 Disc 1/2 全书及逐词片段映射

### 完成

- 解包并清点 `outputs` 中三册 Disc 1/2：Level 1 为 60+65 轨，Level 2 为 65+65 轨，Level 3 为 68+68 轨，共 391 条有效 MP3。
- 依据教材 PDF 45-87 页上印刷的音轨编号，补齐三册 Unit 5-8 与 Review 3-4 的 Disc 2 页码、书页、单元和音轨范围。
- 新增 `curriculum/opw-full-audio-catalog.json`，统一记录 391 轨的整轨区间、尾静音候选点、教材归属、机器转写和逐词起止时间。
- 新增 `curriculum/opw-word-timestamps.json`，保存可续跑的原始逐词识别结果；共生成 14,701 个通过边界检查的词片段。
- 新增本地 Whisper 转写、全书目录构建和校验脚本；不物理切割或复制教材 MP3。
- Level 3 Disc 2 ZIP 中混入的 `OPW_SB3_Disc1_Track68.mp3` 保留原样但排除在 Disc 2 有效库存之外。

### 验证

- 视觉核对三册 PDF 41-88 页 contact sheet，并抽查 Level 3 PDF 56、62-63、81-84 页的一页多轨编号。
- `node scripts/build-opw-full-audio-catalog.mjs`：生成 Level 1 125、Level 2 130、Level 3 136 条记录。
- `node scripts/validate-opw-full-audio-catalog.mjs`：391 轨文件连续、时长一致、页码归属合法；14,701 个词片段均落在源音轨范围内。
- 14,701 个词片段中 3,389 个置信度低于 0.65，目录已明确要求试听复核。
- `git diff --check`：通过。

### 决策 / 已知限制

- 整轨、页码和 section 映射可直接供全书课程生成器使用；逐词时间点是 Whisper tiny.en 机器候选，不等同于人工逐词确认。
- 童谣、拼读音素、重复跟读和低置信度词在进入关键练习前必须试听；目录保留每词置信度支持筛选。
- 教材音频继续只作家庭本地引用，原始 MP3 与 ZIP 不进入 Git 或公开部署包。

### 下一步

- 按课程优先级试听复核低置信度词片段，并把课程生成器从旧 Disc 1 目录切换到全书目录。

## 2026-08-03 · 步骤 31：课程切换到全书目录与优先试听清单

### 完成

- 新增 `curriculum/opw-course-catalog.mjs` 作为课程侧唯一入口，直接读取 `opw-full-audio-catalog.json`，不再使用旧 Disc 1 目录的后半册禁用条件。
- Disc 1/2 section 统一按 `available-disc${disc}` 判定可排课；三册 Unit 1–8、Review 1–4 共 12 个 section 均可生成带光盘号、轨号、源文件和播放区间的课程任务引用。
- 新增 `scripts/generate-opw-listening-review.mjs` 和生成结果 `curriculum/opw-listening-review-priority.json`；清单按“核心词且低置信度、核心词、低置信度”排序，并提供带前后余量的试听区间。
- 核心词当前采用可复现的候选规则：section 明示的拼读重点，或同一活动音轨内重复至少两次且不属于常见教学指令词的词，取首次出现作为代表试听片段；它是机器候选，不冒充教材人工标注。

### 验证

- `node scripts/generate-opw-listening-review.mjs`：生成 4,251 条候选；P0 604、P1 941、P2 2,706，Disc 1/2 分别为 2,137/2,114 条。
- `node scripts/validate-opw-full-audio-catalog.mjs`：391 轨、14,701 个逐词片段通过；三册各 12 个 section 完整映射。
- `node --test tests/opw-course-catalog.test.mjs`：课程入口、全书 section、Disc 2 任务引用和试听清单覆盖测试全部通过。
- `git diff --check`：通过。

### 决策 / 已知限制

- 旧 `opw-disc1-catalog.json` 继续作为全书目录重建时的 Disc 1 历史输入，但不再是课程排课数据源。
- 优先清单只决定试听顺序；人工试听结果需要在下一步单独记录，不能直接覆盖机器置信度。

### 下一步

- 从 P0 开始试听并记录确认结果，优先处理 Level 1 当前要教的 section，再把已确认片段接入孩子端练习。

## 2026-08-03 · 步骤 32：家长侧优先试听审核工具

### 完成

- 在家长中心新增“优先试听审核”，默认筛选 Level 1 P0，可按优先级、册数、Disc 和审核状态切换。
- 审核器复用 IndexedDB 本机音频库，按清单中的 Level/Disc/Track 精确取音轨，只播放带前后余量的短片段；缺少资源时明确提示需要导入的音轨。
- 每条支持“正确、单词错误、边界错误、暂时跳过”，可填写更正或备注；审核结果以稳定片段 ID 保存到浏览器 localStorage，刷新后继续保留。
- 支持导出审核结果 JSON 和撤销单条审核。CloudBase 只部署审核界面代码，不部署优先清单、课程映射或 Oxford 音频，因此实际审核必须在项目本地服务器进行。

### 验证

- `node --check audio-review-core.js`、`node --check audio-review-ui.js`：通过。
- `node --test tests/audio-review-core.test.mjs tests/opw-course-catalog.test.mjs`：7 项测试全部通过。
- 本地浏览器实测：载入 4,251 条清单；Level 1 P0 显示 188 条；Disc 2 筛选定位到 Track 02，并在尚未导入 Disc 2 时给出正确缺轨提示。
- 浏览器保存一条“暂时跳过”后刷新，计数保持 1/4,251；随后通过“撤销本条审核”清理测试记录，恢复 0/4,251。
- 浏览器视觉核对桌面布局，无控制台错误；`git diff --check` 通过。

### 决策 / 已知限制

- 审核结果与机器清单分开保存，避免人工状态被重新生成清单覆盖；导出的结果文件是后续回写 `verified` 状态的事实来源。
- 当前浏览器只检测到三册 Disc 1 已导入。开始审核 Disc 2 前，家长需要从现有本地资源导入对应 Disc 2 文件夹或 ZIP。

### 下一步

- 导入 Level 1 Disc 2，并由家长从 Level 1 P0 开始实际试听；完成首批后导出结果 JSON，再实现审核结果回写与已确认片段接入。

## 2026-08-03 · 步骤 33：修复音频导入后审核器不能立即播放

### 完成

- 修复审核页先检测到缺少音轨、随后在同一页面导入 MP3/ZIP 后，“重听片段”仍保持禁用的问题。
- 音频包导入、Track 05 单轨导入或音频包移除后，音频库现在会通知试听审核器重新读取当前条目。
- 更新本地脚本版本号，避免浏览器继续使用旧的审核与导入界面缓存。

### 验证

- 在全新本地站点数据中复现“缺少 Level 1 Disc 1 Track 3”；导入完整 Disc 1 ZIP 后不刷新页面，按钮立即恢复可用。
- 点击“重听片段”后，Track 03 从 4.40 秒开始播放；播放器 `readyState=4`、时长 51.67 秒且无控制台错误。
- `node --check audio-library-ui.js audio-review-ui.js`、`node --test tests/*.test.mjs` 与 `git diff --check`：通过。

### 决策 / 已知限制

- 当前验证覆盖 ZIP 完整包导入；审核器仍要求当前筛选条目对应的具体 Level、Disc、Track 已导入，缺轨时会保持禁用并显示精确提示。

### 下一步

- 从 Level 1 P0 开始实际试听；需要审核 Disc 2 时先导入对应完整资源包。

## 2026-08-03 · 步骤 34：修正试听反馈的视觉误导与核验确认

### 完成

- 移除“正确”按钮永久绿色的样式；四种结论默认均为中性状态，查看已审核条目时才高亮实际保存的结论。
- 标题区直接显示正确、单词错误、边界错误和跳过的分类计数；提交后明确提示上一条保存的类型。
- “撤销本条审核”改为“撤销最近记录”，在自动跳到下一条后仍可撤销刚才的操作。
- 增加核验说明：机器词错误选“单词错误”；词正确但片段多带、少带或截断选“边界错误”。

### 验证

- 读取现有本地审核数据确认：4 条记录中正确 0、单词错误 2、边界错误 2；旧界面只是视觉上把“正确”永久标绿，数据分类没有丢失。
- 切换到“已审核”后，边界错误条目仅高亮“边界错误”，单词错误条目仅高亮“单词错误”，并显示对应文字确认。
- `node --check audio-review-ui.js`、`node --test tests/*.test.mjs` 与 `git diff --check`：通过。

### 决策 / 已知限制

- 每条当前保存一个主要结论；若单词和边界同时有问题，选择影响更大的结论，并在备注中补充另一项问题。

### 下一步

- 继续审核 Level 1 P0；阶段性切到“已审核”抽查高亮结论并导出 JSON 备份。

## 2026-08-03 · 暂停检查点：试听审核待数小时后继续

### 当前状态

- 当前浏览器已保存 4/4,251 条审核记录：正确 0、单词错误 2、边界错误 2、跳过 0；记录仍保存在该浏览器的本地存储中。
- 页面已修复导入后不能立即播放，以及“正确”按钮永久标绿造成反馈误判的问题；已审核条目会高亮真实保存的结论。
- 本次测试会话已导入 Level 1 Disc 1（60 轨）和 Disc 2（65 轨）完整资源；音频只保存在当前浏览器。
- 当前课程 section `Level 1 Disc 1 · Aa Bb Cc` 有 P0 21 条、P1 30 条、P2 77 条候选；全清单共 P0 604、P1 941、P2 2,706 条。

### 继续方式

- 回到本地页的家长中心，保持 `Level 1 / P0 / 未审核`，先完成当前 `Aa Bb Cc` section 的 21 条 P0；不需要单独导入每一个片段。
- 每张 Disc 只需导入一次完整 MP3 文件夹或 ZIP，审核器会按清单自动读取相应 Track 和时间范围。
- 每完成一小批即切到“已审核”抽查分类，并点击“导出审核结果 JSON”做阶段备份。
- 只优先人工核验即将用于孩子练习的 section 和 P0；P1、P2 后续按实际课程需要分批处理，不要求一次核完 4,251 条。

## 2026-08-04 · 步骤 35：Level 1 P0 审核回收与问题复核

### 完成

- 读取 `../outputs/opw-listening-review-results-2026-08-04.json`，确认 Level 1 P0 已审 188 条：正确 132、单词错误 47、边界错误 9、跳过 0。
- 新增“问题待复核”筛选，只显示单词错误和边界错误；改判为正确后自动从问题列表移除。
- 问题条目第二轮播放改用更宽的上下文窗口：原词前 0.50 秒、后 0.65 秒；首次审核仍使用前 0.15 秒、后 0.20 秒。
- 审核记录现在保存实际试听窗口，后续可以区分“原始词时间戳”和“人工确认时使用的播放边界”。
- 保留稳定条目 ID 和浏览器已有 188 条结果，不要求重审 132 条正确记录。

### 验证

- `node --check audio-review-core.js audio-review-ui.js scripts/generate-opw-listening-review.mjs`：通过。
- `node --test tests/*.test.mjs`：11 项全部通过。
- `git diff --check`：通过。
- 重新生成试听清单：总数仍为 4,251，P0/P1/P2 数量不变，所有条目新增扩展试听窗口。

### 决策 / 已知限制

- 本轮 56 条错误记录没有备注，无法可靠推断正确单词或精确边界；第二轮只提供更充分上下文，不自动伪造更正词。
- 当前扩展窗口是保守的统一规则，可能包含相邻提示；第二轮确认结果会作为后续生成正式孩子练习片段的事实来源。

### 下一步

- 在本地页面选择 `Level 1 / P0 / 问题待复核`，只复核 56 条；完成后再次导出 JSON，再据此生成 Level 1 已确认片段集。

## 2026-08-04 · 步骤 36：Level 1 人工确认片段集

### 完成

- 读取第二次导出的 `../outputs/opw-listening-review-results-2026-08-04 (1).json`：188 条中正确 143、单词错误 42、边界错误 3。
- 第二轮从原 56 条问题记录中恢复 11 条可用片段；其余 45 条按用户决定从课程可用候选中排除。
- 新增 `scripts/build-opw-verified-clips.mjs`，将审核结果与原试听清单、全书轨道目录合并。
- 生成 `curriculum/opw-level1-verified-clips.json`，只包含 143 条人工确认的 Level 1 P0 片段；42 条词错和 3 条边界错不进入 `items`。
- 第二轮确认的 11 条使用人工复核时实际听到的扩展窗口；第一轮正确的 132 条沿用初始试听窗口。

### 验证

- 188 条审核结果全部能映射回稳定候选 ID。
- 最终文件 143 条 ID 唯一、全部为 Level 1、所有播放区间均为有效正区间。
- 原始 Oxford MP3 只被引用，没有修改或删除。

### 决策 / 已知限制

- “删除”仅指从可用于课程的片段集合中排除，不删除原始音频和审核证据。
- 45 条排除项仍保留在导出的 JSON 中，未来如有更好的转录或精确标注方法可以追溯恢复。

### 下一步

- 用 `opw-level1-verified-clips.json` 生成孩子端 Level 1 课程顺序，先做一个 section 的听音选择与复习闭环，再扩展全册。

## 后续记录模板

每完成一个步骤，在本文件末尾追加：

```markdown
## YYYY-MM-DD · 步骤 N：名称

### 完成
- ...

### 验证
- 命令或操作：结果

### 决策 / 已知限制
- ...

### 下一步
- 一个明确、可直接执行的动作
```

## 2026-08-04 · 步骤 37：首周自有课程与最小音频缺口

### 完成

- 以自有教学顺序制定首周 7 天课程：短元音 a、a 的 -at/-ap 词族、i、e、o、u，以及第 7 天短元音混合复习；Oxford Level 不再作为课程顺序。
- 从 RAZ `4-Week Plan` 第 1 周逐日选定 1 本锚点书，同时保留原表的辅助书、目标词、句型和家长任务。
- 每个正式教学日安排 4 个可拼读词、心词新学/复习和 1 个听音选择活动；第 6–7 天遵守原计划“不新增、轻松输入”，只做复习。
- 新增 `scripts/build-week1-course.mjs`，可重复生成 `curriculum/week1-course.json` 和 `curriculum/opw-week1-review-queue.json`。
- 24 个不重复拼读词中，`ant/cat/dog/cup/sun/up` 复用 6 个 Level 1 人工确认片段；其余 18 个词每词只选 1 个 Level 2 候选进入首周审核清单。

### 验证

- `node scripts/build-week1-course.mjs`：生成 7 天课程；6 个词已确认、18 个词待审核、0 个无素材。
- 首周审核清单共 18 条，全部来自 Level 2；Disc 1 为 10 条、Disc 2 为 8 条，Level 3 本周无需审核。
- `node --test tests/week1-course.test.mjs`：4 项通过，覆盖课程顺序、RAZ 锚点、每日结构、已确认/待审核边界和清单唯一性。

### 决策 / 已知限制

- `week1-course.json` 中只有 `audio.status=verified` 的片段可进入孩子端；18 个 `pending-review` 候选在人工确认前不得播放给孩子。
- 首周心词按 5 个正式教学日引入 `I/you/where/what/my`；第 6 天周回收和第 7 天轻松输入不新增，避免为了满足机械数量破坏原 RAZ 节奏。
- 当前课程数据已经落地，但孩子端活动仍未启用；要先完成 18 条首周专用音频审核并回写结果。

### 下一步

- 让家长审核页默认载入 18 条首周专用清单，使用独立审核状态，完成后导出结果并生成首周已确认音频集。

## 2026-08-04 · 步骤 38：首周专用审核入口

### 完成

- 家长中心“优先试听审核”改为“首周试听审核”，默认读取 `opw-week1-review-queue.json`，不再要求从 4,251 条全量候选中筛选。
- 首周审核使用独立 localStorage 键，不读取或覆盖此前 188 条 Level 1 审核结果。
- 默认筛选调整为“全部 18 条 / Level 2 / Disc 1+2 / 未审核”，条目标题显示对应课程日和“课程必需词”。
- 导出文件名和 `sourceQueue` 改为首周专用命名，后续可明确回写到 `week1-course.json`。
- 已把本地 Level 2 Disc 2 的 65 条 MP3 导入当前浏览器音频库；Disc 1/2 共 18 条首周候选现均可直接试听。

### 验证

- `node --check scripts/build-week1-course.mjs audio-review-ui.js`：通过。
- `node --test tests/*.test.mjs`：16 项全部通过；`git diff --check`：通过。
- 本地浏览器实测：显示 `已审 0/18`，第一条为第 1 天 `man`，Level 2 / Disc 1 / Track 16；旧 Level 1 结果未混入，控制台无错误。
- 切换到 Disc 2 后显示 1/8，第一条为第 3 天 `hit`，重听按钮处于可用状态；资源列表显示 Level 2 Disc 2 共 65 条、54.1 MB。
- 视觉核对桌面布局：状态汇总、四组筛选和审核卡片均完整可读，没有溢出或遮挡。

### 决策 / 已知限制

- Level 2 Disc 1/2 均已导入当前本地测试浏览器；音频不会进入代码仓库，若清除浏览器网站数据仍需重新导入。
- 这 18 条仍是机器候选；只有家长点击“正确”的结果才允许进入首周孩子端课程。

### 下一步

- 人工审核首周 18 条并导出 `opw-week1-review-results-2026-08-04.json`；随后回写首周已确认片段并启用孩子端活动。

## 2026-08-04 · 步骤 39：回写首周第一轮审核并生成修订清单

### 完成

- 读取第一轮 18 条人工审核结果：12 条正确、4 条边界错误、2 条单词错误。
- 新增 `scripts/apply-week1-review-results.mjs` 和 `curriculum/opw-week1-review-state.json`；12 条通过项正式进入首周课程，课程状态变为 18 个词可用、6 个词待复核。
- `man/pet/in/it` 去掉统一试听余量，改用机器逐词时间点的精确起止边界。
- `red` 改用 Level 2 Disc 1 Track 43 的完整词 `39.62–40.82`；`pin` 改用已导入的 Level 2 Disc 2 Track 31 中完整词 `63.36–64.04`，无需额外导入 Level 3 音频。
- 二次清单使用 `reviewRevision: 2` 和 `-r2` ID；审核统计与导出只读取当前清单，避免浏览器中第一轮记录污染二次审核。

### 验证

- `node --test tests/*.test.mjs`：18 项全部通过。
- `node --check audio-review-core.js audio-review-ui.js scripts/apply-week1-review-results.mjs scripts/build-week1-course.mjs`：通过。
- `git diff --check`：通过；Oxford 源 MP3 未修改或删除。

### 下一步

- 在家长中心刷新后试听当前 6 条 `-r2` 修订项；全部确认后再次导出 JSON 并运行同一回写命令。

## 2026-08-04 · 步骤 40：结束首周音频复核

- 二次审核 6 条全部完成：`red`、`pin` 通过并进入孩子端课程。
- `man`、`pet`、`in`、`it` 二次审核仍有串音，按家长决定停止继续修边界并从孩子端音频中排除；文字教学内容保留。
- 最终首周 24 个词中 20 个有可靠音频、4 个无音频、0 个待审核，不再生成第三轮审核清单。

## 2026-08-04 · 步骤 41：家庭私有云端音频链路

### 完成

- 确认正式 CloudBase 站点本身已有平台 HTTPS 域名，不需要家庭配置证书；重复导入问题来自英语实验版只使用浏览器 IndexedDB，不是 HTTPS 限制。
- 为实验 Supabase 增加私有 `family-audio` bucket 与 RLS：同一家庭成员可读，只有 owner 设备可上传或替换；对象按 `<family-id>/assets/<asset-id>.mp3` 隔离。
- `CloudStore` 新增音频列表、上传和 1 小时签名 URL；`AudioStore` 保持本机优先，在本机缺轨时自动回退家庭云端，因此课程、审核器和 Track 05 无需分别重写播放器。
- 家长中心新增“上传全部到家庭云端”：自动跳过云端已有音轨，并显示进度、音轨数和容量。IndexedDB 继续作为离线缓存。
- 新增独立补丁 `supabase-family-audio-storage.sql`，同时把完整初始化内容合入 `supabase-schema.sql`。

### 验证

- `node --check cloud-store.js audio-store.js audio-library-ui.js`：通过。
- `node --test tests/*.test.mjs`：22 项全部通过。
- `git diff --check`：通过。
- 尚未创建实验 Supabase、执行线上 SQL、上传 Oxford MP3 或部署实验 CloudBase；本步骤没有产生外部云端变更。

### 决策 / 已知限制

- 继续禁止英语实验版连接正式家庭数据库；必须使用独立实验 Supabase 项目。
- CloudBase 发布包仍不含 Oxford MP3；教材音频由家庭 owner 设备直接上传到私有 Storage。
- 首次上传约 223 MB，耗时取决于上行带宽；后续上传会按资源 ID 跳过已有文件，其他手机不再重复导入。

### 下一步

- 创建独立实验 Supabase 项目，执行 schema 并填入 publishable 配置；随后上传并用第二台设备验证签名播放。

## 2026-08-05 · 步骤 42：Vercel Preview 与云端链路检查点

### 完成

- 免费方案改为复用现有 Supabase 家庭项目，并只发布到现有 Vercel 项目的 Preview；未创建第二个 CloudBase 环境，也未启用任何付费功能。
- 已在线执行 `supabase-family-audio-storage.sql` 增量补丁，创建私有 `family-audio` bucket 及家庭成员读取、owner 上传策略；未修改现有家庭任务数据。
- 英语版公开配置已接入现有 Supabase publishable 配置；本地 22 项自动测试全部通过。
- Vercel Preview 已成功发布并打开：`https://summer-task-dashboard-elo5hrok0-homework-ef24.vercel.app`。页面主体与现有家庭任务正常加载，Production 和正式域名均未更新。
- Preview 中已确认云端连接可用，并成功创建临时验证家庭空间“`双星学习岛升级验证`”。

### 当前卡点

- 家长中心和音频导入控件已定位，但浏览器自动化在选择本地测试 MP3 时断开；尚未完成“小文件上传 → 云端对象列表 → 签名 URL 读取/播放”闭环。
- 2026-08-05 续查：Preview 仍可在已登录 Chrome 中正常打开，状态显示“云端已同步”；家长中心确认家庭云端当前为 0 条音频。分别尝试文件夹选择器与单条测试 ZIP，浏览器均拒绝自动注入本地文件，因此卡点已确认是浏览器文件选择安全限制，不是 Preview、Supabase 连接或页面脚本报错。
- 因上述闭环尚未通过，当前 Preview 不能提升为 Production，也还未进行第二台设备验证或 223 MB 全量音频上传。

### 下一步

- 重新连接 Preview，从家长中心上传一条最小测试 MP3，验证私有 Storage、签名 URL 和播放。
- Preview 页面已保留在 Chrome；需要家长在页面上手动选择一次测试 ZIP/MP3，之后可继续自动验证上传、签名 URL 与播放结果。
- 删除或保留临时验证家庭空间需在验证后决定；通过后再做第二设备验证，最后才评估 Production 发布。

## 2026-08-05 · 步骤 43：完成家庭云端音频与第二设备闭环

### 完成

- 家长在原 Preview 导入 Level 1/2 的四张光盘，共 255 条、约 226 MB；首周课程音频覆盖为 19/19。
- 通过“上传全部到家庭云端”把 255 条音频写入私有 `family-audio` bucket。数据库只读核对结果为 255 条、225.6 MB，全部属于临时验证家庭“`双星学习岛升级验证`”。
- 使用固定 Preview 别名 `https://summer-task-dashboard-megansunxue-1087-homework-ef24.vercel.app` 模拟第二设备：该浏览器没有本机 Oxford 音频，加入验证家庭并刷新后，首周可用词从“需导入”变为“可播放”。
- 第二设备实际播放验证通过：播放器从 `ftaryydrcbmequqvmztr.supabase.co` 获取 HTTPS 签名 URL，`readyState=4`、无媒体错误、播放状态正常。
- 修复刷新后家庭邀请码丢失：`CloudStore.init()` 现在每次启动都重新读取成员关系元数据；刷新后邀请码仍能显示。
- 增加全局 `[hidden]` 样式，已连接设备只显示邀请码，不再同时暴露“创建/加入家庭”表单。

### 验证

- `node --check cloud-store.js`、`node --test tests/*.test.mjs`、`git diff --check`：全部通过，共 22 项测试。
- 固定 Preview 回归：云端状态为“云端已同步”，邀请码刷新后显示，连接表单隐藏，第二设备签名音频持续可播放。
- 原项目最新 Preview 部署为 `https://summer-task-dashboard-5xrgx6nhn-homework-ef24.vercel.app`；固定 Preview 别名已指向该部署。原正式站点的 Production 别名未更改。

### 待决定

- 一次发布命令在错误目录执行，意外新建独立 Vercel 项目 `summer-task-dashboard-english`；其首次部署被 Vercel 自动标记为该新项目的 Production，并分配 `https://summer-task-dashboard-english.vercel.app`。它没有覆盖原正式项目，但属于额外外部资源，删除前需家长确认。
- 临时验证家庭及其 225.6 MB 私有音频目前保留，用于后续手机实测；确认正式迁移方案后再决定是否保留或删除。

## 2026-08-05 · 步骤 44：每日英语恢复三线整合并扩充练习量

### 问题与决策

- 家庭反馈指出孩子端实际只练自然拼读，内容太少且偏简单。核对原规划后确认课程应同时包含自然拼读、心词/高频词和 RAZ 阅读迁移，并保留听力输入。
- `curriculum/week1-course.json` 其实已经包含心词、RAZ 目标词、句型和锚点书；缺口在 `buildRounds()` 只把 `phonics.words` 转成孩子端练习。因此本步骤沿用既有课程事实，不另造一套数据。
- 第 1–5 天各 13 轮，第 6 天 12 轮，第 7 天 10 轮；周末继续遵守“不新增、轻松输入”的原计划。

### 完成

- 每日课程改为连续四段：声音实验室 → 心词锻造屋 → RAZ 目标词与开口句型 → 锚点书阅读。
- 心词加入“新词整体识记 + 句中选词”题型；RAZ 加入 8–10 个目标词预读、2–3 个句型开口和锚点书完成任务。
- 课程卡首页直接展示三条课程线及当天 RAZ 锚点书；完成日志能区分拼读词、目标词、句型和书目。
- 修复无本机/云端 Oxford 音频时的降级流程：可播放词改为看词拼读后，“我读好了”现在可以正常前进。

### 验证

- `node --check week1-course-core.js week1-course-ui.js`：通过。
- `node --test tests/*.test.mjs`：24 项全部通过；新增测试覆盖完整三线轮次及心词语境题。
- `git diff --check`：通过。
- 本地浏览器真实完成第 1 天 13/13：4 个拼读词、1 次新心词识记、4 个心词选择、1 次 RAZ 目标词预读、2 个开口句型和 1 本锚点书；桌面视觉检查通过。

### 当前边界

- 本步骤补齐的是首周孩子端课程体验；尚未生成第 2–4 周的同类交互课程。
- RAZ 锚点书目前是阅读任务与目标词/句型练习，不包含书内逐页音频播放器；后续可基于 RAZ 素材授权和现有目录继续增强。

## 2026-08-05 · 步骤 45：完成第 2–4 周英语岛课程

### 课程事实与决策

- 用表格运行时读取 `outputs/raz_bf_catalog/raz_b_to_f_catalog.xlsx` 的 `4-Week Plan!A1:H35`，第 2–4 周书目、主题、目标词、句型和家庭任务均以该表为事实来源。
- 自然拼读按认知台阶递进：第 2 周辅音组合（sh/ch/th/wh/ng/nk），第 3 周首尾辅音丛，第四周长元音与 Silent e；没有继续重复首周短元音。
- 每周前 5 天引入新心词，第 6 天回收、第 7 天轻松输入；RAZ 始终保留目标词预读、句型开口和锚点阅读。

### 完成

- 新增 `scripts/build-english-course.mjs` 与 `curriculum/english-course.json`：共 4 周、28 天、82 个不同拼读词、20 个正式教学日和 8 个回收/轻松日。
- 第 2–4 周每天为 10–16 轮，统一复用声音实验室、心词锻造屋和 RAZ 故事森林，不新增割裂入口。
- 课程日期映射从 7 天扩展到 28 天；首周沿用旧 activity ID，已保存的首周进度不会丢失，第 8 天起使用新的四周课程 ID。
- 心词语境题补齐 many/more/first/next/then、time/want/need/because/with、who/when/why/how/does/said。
- 家长中心按钮改为可在家长端和孩子端之间往返，家长选择未来日期后能直接预览对应周课程。
- 未人工确认的第 2–4 周单词音频不冒充可用音频，孩子端明确降级为看词拼读；后续可按真实使用优先级逐批审核音频片段。

### 验证

- `node --check scripts/build-english-course.mjs app.js week1-course-core.js week1-course-ui.js`：通过。
- `node --test tests/*.test.mjs`：31 项全部通过；新增覆盖 28 天连续性、四周难度递进、RAZ 主题锚点、每周心词节奏、每日混合轮次和音频安全边界。
- `git diff --check`：通过。
- 本地浏览器回归第 8、15、22 天：第 2 周 `sh + 数量比较`、第 3 周 `s 辅音丛 + 学校`、第 4 周 `a_e + 疑问词` 均正确显示；第 8 天实际推进到 5/14 并进入 many 语境题。
- 第 4 周桌面视觉检查通过；浏览器控制台无 warning/error。

### 下一步

- 先让孩子按实际进度使用第 2 周；记录过简单、过难和犹豫词，再决定优先做错词间隔复习，还是先审核第 2 周 Oxford 单词音频。

## 2026-08-05 · 步骤 46：课程音频改为电脑端一次发布

### 决策与完成

- 撤销 owner 手机导入、浏览器 IndexedDB 持久化和家庭 Storage 上传这条运行时链路；家长页不再显示任何音频上传控件。
- 新增 `scripts/build-static-course-audio.mjs`，从 28 天课程中提取 19 条已审核音轨引用，只复制这些源 MP3 并生成 `course-audio/manifest.json`，合计 19.4 MB。
- `deploy-cloudbase.sh` 在每次发布前从电脑源文件重建最小音频包，并将其与网站一起发布到固定 `/english/` 子目录。
- `AudioStore` 优先读取站内清单和静态 MP3；孩子设备不需要家庭身份、本机文件或缓存即可播放。
- 课程轮次为 0 时不再误判为“完成 0/0”，而是禁用开始按钮并显示课程数据错误；相关脚本版本已提升以绕过旧缓存。
- 远程 MP3 加载完成后强制跳到人工审核的片段起点，并继续按片段终点自动停止，避免从整条音轨开头播放。

### 验证与发布

- `node --test tests/*.test.mjs`：31 项全部通过；`node --check` 与 `git diff --check` 通过。
- 本地真实浏览器：第 1 天显示 `0/13`；Track 04 从 27.83 秒播放并在 29.71 秒自动停止，无控制台或媒体错误。
- CloudBase 已上传 36 个文件，其中包含清单和 19 条 MP3；线上清单报告 19 条、20,296,539 bytes，MP3 返回 `audio/mpeg`。
- 线上真实浏览器：第 1 天显示 `0/13`、三个已审核词显示“可播放”；点击开始后静态 Track 04 `readyState=4`，定位至片段并在 29.72 秒停止，媒体错误为空。

## 2026-08-05 · 步骤 47：核心高频词扩充到 100 个

### 决策与完成

- 联网参考 University of Florida Literacy Institute 的 irregular/high-frequency word 教学说明和 Dolch 基础词表，不把全部 RAZ 主题词机械地当作心词。
- 根据孩子当前接受程度，将 Dolch 低龄基础高频词与 RAZ 锚点词整合为 100 词。
- 20 个正式教学日每天新增 5 个；每个新词分别经过读写 3 遍、遮住答案后的句中拼写，答对才能继续；每天再安排最近词与间隔词复习，最多 10 个。
- 课程数据新增 `newWords` 以支持同日多个新词；旧 `new` 字段继续保留，首周已有课程和进度标识保持兼容。轮次结构版本提升，避免旧轮次序号跳过新增练习。
- 孩子端明确显示“本期核心高频词总表 · 100 词”。

### 验证

- `node scripts/build-english-course.mjs`：生成 26 天、100 个核心高频词、93 项 RAZ 书目安排。
- `node --check scripts/build-english-course.mjs week1-course-core.js week1-course-ui.js`：通过。
- `node --test tests/*.test.mjs`：35 项全部通过；覆盖每天 5 个新词、100 词总量、多新词书写与独立拼写。
- 本地浏览器回归：教学日正确显示 5 个“新词·要写”、5 个昨日词“复习·要拼”和“本期核心高频词总表 · 100 词”；当天共 25 轮，页面无加载错误。

## 2026-08-05 · 步骤 48：发布 26 天正式课程

### 发布与线上验证

- 发布前重新生成课程并通过 35 项自动测试、JavaScript 语法检查与 `git diff --check`。
- CloudBase 固定环境 `twin-start-island-d7dxg7911b0684` 的 `/english/` 路径成功上传 36 个文件，包含 26 天课程和 19 条静态音频。
- 线上回读确认课程为 26 天、100 个核心高频词；第 1 天与第 26 天数据完整。
- 抽查静态 MP3 返回 HTTP 200、`audio/mpeg`，课程音频清单共 19 条。

## 2026-08-05 · 步骤 49：第 2–4 周 Oxford 音频定向扫描（仅 Level 1–3）

### 范围与决策

- 沿用本地 `whisper-tiny.en-mlx`，不下载新模型；本轮只扫描现有 Oxford Phonics World Level 1–3，不再查找或依赖 Level 4–5。
- 定向转写按音轨断点保存。第 2、3 周分别对课程旧清单未命中的 14、16 个词扫描三册全部音轨；第 4 周先扫描 Level 3 Unit 1–3，再对仍缺的 `ship/frog/hand/plan` 扫描三册全部音轨。
- 定向结果与旧无提示时间戳合并，避免用“缺词扫描结果”覆盖并丢失旧清单中原本有效的 `thin/sing` 等命中；同一位置优先使用定向结果。
- Whisper 精确文本命中只生成试听候选，不直接成为正式课程音频。低置信度 `hand` 经旧无提示转写逐秒交叉核对，实际为 `hen`，已判定为提示诱导假命中并排除。

### 扫描结果

- 第 2 周：20 个目标词中 6 个有候选，共 16 段；命中词为 `fish/king/pink/sing/thin/which`。其余 14 个无可靠精确命中：`ship/shop/dish/chin/chat/much/rich/bath/moth/path/when/whip/whiz/ring`。
- 第 3 周：19 个目标词中 3 个有候选，共 9 段；命中词为 `milk/nest/swim`。其余 16 个无可靠精确命中：`stop/step/spin/spot/clap/flag/slip/plan/frog/trip/drum/crab/hand/jump/black/plant`。
- 第 4 周：24 个目标词中 20 个有候选，共 59 段，其中 55 段达到机器试听门槛，4 段需优先重转写/复核；仍缺 `ship/frog/hand/plan`。
- 候选分布符合教材边界：第 2 周主要由 Level 1 提供，第 3 周仅少量旧词命中，第 4 周 Silent e 主要由 Level 3 提供。Level 1–3 本身不系统覆盖第 2 周 digraph 与第 3 周 blend，因此未命中不能靠提示强行补齐。

### 产物与当前状态

- 定向时间戳保存在 `tmp/course-audio-transcripts/week-{2,3,4}-l{1,2,3}.json`；三册每轮分别扫描 122、127、133 条音轨。
- 可试听清单保存在 `tmp/course-audio-review/week-{2,3,4}/review-queue.json`，对应短片段位于各周 `clips/` 目录；候选构建脚本为 `scripts/build-course-audio-review-candidates.mjs`。
- 当前结果是“机器候选队列”，尚未完成人耳逐段确认，也未写入正式课程音频清单。下一步应在现有审核页试听并确认，优先处理第 4 周 4 段低质量候选，再审核其余候选；无候选词继续使用孩子端看词拼读降级，不伪造可播放状态。

## 2026-08-05 · 步骤 50：恢复第 2–4 周人工试听审核页

### 纠偏

- “审核”继续沿用既定的人工作业流：家长在页面试听并标注，导出结果 JSON，再由脚本生成正式课程音频；机器重转写只用于生成和排查候选，不能代替人耳结论。
- 正式家长页此前已撤销音频导入区，旧审核脚本虽仍存在但没有可进入的 DOM 面板。为避免重新引入已废弃的整轨 IndexedDB/云端音频链路，本步骤新增独立的本地审核页面，直接播放本轮已生成的短 MP3。

### 完成

- 新增 `audio-review.html`，顶部可切换第 2、3、4 周，保留“正确 / 单词错误 / 边界错误 / 暂时跳过”、备注、问题复核、撤销和 JSON 导出。
- `audio-review-ui.js` 按 `?week=2|3|4` 读取对应 `tmp/course-audio-review/week-N/review-queue.json` 与 `clips/`；三周使用互不混淆的浏览器本地进度键，导出文件名包含周次。
- 第 2–4 周短片段不再要求先导入 Oxford ZIP；旧首周/整轨审核仍保留原 `AudioStore` 路径。

### 验证与当前交接

- `node --check audio-review-ui.js`、8 项审核核心测试和 `git diff --check` 通过。
- 浏览器真实回归：第 2、3、4 周分别载入 16、9、59 条，首条 MP3 地址正确；第 4 周首条 `make` 的音频 `readyState=4`、播放到底无媒体错误。
- 第 4 周“正确”标注后统计由 0/59 变为 1/59，撤销后恢复 0/59，确认保存与撤销链路正常且未留下测试标注。
- 本地服务器运行于 `http://127.0.0.1:4174/`；审核页已打开在 `http://127.0.0.1:4174/audio-review.html?week=4`，等待家长人工试听。

## 2026-08-05 · 步骤 51：应用第 2–4 周人工审核结果

### 审核结论与课程回写

- 三周审核 JSON 均完整且 `pending: 0`：第 2 周 16 条、第 3 周 9 条、第 4 周 59 条，共审核 84 个候选。
- 59 个候选标记为正确；按“同一周同一单词只选一个，优先候选排名、置信度和短边界”的既定规则去重后，生成 24 个正式词片段。
- 第 2 周新增 `fish/king/pink`，第 3 周新增 `milk/nest/swim`，第 4 周新增 18 个词；错误、边界问题、跳过和无候选词继续安全降级为看词拼读。
- `scripts/apply-course-audio-review-results.mjs` 已生成 `curriculum/opw-weeks2-4-verified-clips.json` 并重建 `curriculum/english-course.json`；课程状态为 `human-review-applied`。
- 静态课程包由 19 条音轨扩为 40 条、43.4 MB；孩子端和家长任务中的“待人工审核”旧提示已改为“仅播放人工确认音频”。

### 验证与下一步

- `node --check`、36 项 `node --test tests/*.test.mjs` 与 `git diff --check` 全部通过。
- 本地课程生成和静态音频包已就绪；下一步是发布到 CloudBase `/english/`，随后在线抽查课程 JSON、音频清单和第 2–4 周各一个新片段。
## 2026-08-07 · 自有拼读音频候选与质量闸门

### 完成

- 不改变“短元音 → digraph → blends → Silent e”的自有课程顺序，为当前所有 39 个无可用音频的不同单词生成 `Samantha` 美式英语、145 wpm 的独立 TTS 候选。
- 新增 `tts-audio-review.html`：逐词试听、通过/拒绝、备注与审核结果导出；候选未经人耳确认不会进入孩子端。
- 新增人工录音替换入口 `human-recordings/phonics-words/`。同名单词录音优先于 TTS，统一转换为单声道 MP3、标准音量和安全的前后留白后重新审核。
- 课程音频支持两类资产：Oxford 整轨片段与独立单词文件。选择顺序为已审核 Oxford → 已审核人工录音/TTS → 看词拼读降级。

### 验证

- 39/39 个候选文件非空，时长 0.582–0.924 秒；试听页载入 39 个对应卡片与音频地址。
- `node --test tests/*.test.mjs`：37 项全部通过。
- 当前 `verifiedFallbackWords=0`，说明质量闸门有效；必须导入人耳审核结果后才会启用候选。

### 人工审核结论与长期策略

- 用户逐词试听 39 个 TTS 候选后确认全部通过；系统以 `user-confirmed-all-in-conversation` 保存完整审核记录，不要求再次下载、上传 JSON。
- 39 个文件正式写入课程音频源并进入静态音频包，课程中的每个自然拼读词现均有已审核音频。
- 后续新增课程词默认走“生成 TTS → 人耳试听 → 接入课程”；不再先扫描 Oxford 全轨。已有已审核 Oxford 片段保留，若个别 TTS 不理想则用同名人工录音替换。

## 2026-08-07 · 常见自然拼读规律压缩并入 26 天课程

### 课程调整

- 保持 26 天和原有“短元音 → 辅音组合 → 辅音丛 → Silent e”主线不变，在每天核心词后增加 2 个常见规律拓展词；所有日期最多 8 个拼读词。
- 新增 46 个不同词，覆盖四组高价值规律：`ck/qu/tch/ph`；`ai/ay/ee/ea/oa/ow`；`ar/or/er/ir/ur`；`oo/ou/ow/oi/oy`。
- 课程不同拼读词由 82 个增至 128 个；课程 schema 和轮次 schema 均提升，旧的进行中轮次会安全重置，已完成日期与其他家庭进度不受影响。

### 音频状态

- 已生成 46 条 Samantha 美式英语 TTS 候选，全部保留为 `pending-human-review`；人工试听前孩子端使用看词拼读，不会把候选冒充为已审核音频。
- 修复音频审核应用脚本：后续批准新候选时会保留已有 39 条已审核单词音频，不再用本轮结果覆盖旧词库。

## 2026-08-07 · TTS 自动入课版发布

### 发布结果

- 按新规则将 TTS 改为生成后自动接纳；人工录音仍保留明确审核边界。
- `deploy-cloudbase.sh` 重建 26 天课程、生成/接纳 TTS、构建最小静态音频包后，发布到既有 CloudBase 环境的 `/english/` 子目录。
- CloudBase CLI 3.7.2 成功上传 142/142 个文件；线上地址为 `https://twin-start-island-d7dxg7911b0684-1461490716.tcloudbaseapp.com/english/`。

### 线上验收

- 线上课程 JSON 与本地发布文件 SHA-256 完全一致：26 天、128 个不同拼读词、164 次按日拼读练习、100 个心词、93 项 RAZ 分配。
- 164/164 次拼读安排均有 `verified` 音频；静态清单包含 125 个音频资产。
- 首页、课程脚本、课程 JSON、音频清单以及新规则词 `back` 的 MP3 均返回 HTTP 200；抽查 MP3 与本地文件 SHA-256 一致。
- 发布前通过 JavaScript 语法检查、39 项自动测试和 `git diff --check`。

## 2026-08-07 · 英语岛合并正式主站与任务联动修复

### 产品决定

- 不长期保留主站和 `/english/` 两套入口；英语岛并入双星学习岛正式主入口，`/english/` 仅作为发布前验收路径。
- 正式主入口发布并验收后，`/english/` 首页改为跳转主入口，避免两套地址继续产生分叉使用习惯。

### 任务与计划修复

- 8 月手部休养计划截至 8 月 6 日；旧区间若延伸到 8 月 7 日以后会自动截短或移除。8 月 7 日起哥哥、弟弟恢复六项总体默认任务。
- 默认任务中的旧“英语 RAZ”替换为“英语岛 · 今日课程”，完整包含自然拼读、心词拼写和按表 RAZ 复习。
- 总体任务发布不再跳过已有完成记录的当天/未来生成任务；重建时按任务 ID 保留完成状态，并立即刷新孩子端。
- 家长按钮与反馈改为“发布到孩子端”，明确这是会影响孩子视图的操作。

### 数据迁移

- 新版同时识别旧主站和 `/english/` 的 localStorage 与家庭标识；两份本地状态同时存在时做保守合并，不简单选一份覆盖另一份。
- 历史日期保留；同名任务完成状态取并集；错题、备注、花园、总体设置及英语岛开课/练习进度均保留。
- 继续复用原 Supabase `family_states`，没有删除表、家庭或进度数据，也没有新建第二套家庭后端。

### 验证与发布

- 新增数据合并自动测试；JavaScript 语法检查、40 项自动测试及 `git diff --check` 全部通过。
- 浏览器真实回归：哥哥、弟弟在 2026-08-07 均显示 6 项任务、第一项为英语岛；旧“英语 RAZ”为 0。家长修改英语岛总体名称并发布后，孩子端立即显示新名称；恢复后立即还原。
- `/english/` 与正式主入口分别成功上传 143/143 个文件；正式线上首页、应用代码、迁移模块、课程 JSON 与抽查音频均返回 HTTP 200，且 SHA-256 与本地完全一致。
- 正式线上课程仍为 26 天、128 个不同拼读词、100 个心词，拼读音频不可用条目为 0；浏览器控制台无错误。
- `/english/` 入口已改为跳转正式主入口，线上跳转与英语岛渲染验证通过。

## 2026-08-07 · 记录 26 天后的长期产品需求

### 已确认的产品方向

- 双星学习岛定位为长期家庭学习工作台，当前暑假 26 天英语课程只是第一期内容，不是产品使用期限或固定课程模板。
- 家长中心需要支持一次性和短周期临时任务，可按自定义日期安排非连续学习节奏，例如 3–4 次、隔一天学习一次的舞蹈练习；临时任务到期后不改写长期课程。
- 后台需要支持“总体学习目标 → 阶段课程 → daily 学习任务”的拆解、预览与发布，孩子端继续只显示当天可执行任务。
- 英语课程按学习阶段演进。当前自然拼读在完成基础阶段后可以退出，由下一阶段的新内容接替；前端不能固化当前 26 天或自然拼读模块。
- 所有后台规划的课程内容汇总到 Excel 总表，以总表作为审核、排期和发布的主要事实来源，再生成前端课程数据；家长临时任务属于独立的灵活发布层。
- TTS 生成与课程资源关联流程继续复用，并与自然拼读解耦，服务于后续任何需要语音的学习内容；人工录音仍可作为替换资源。

### 本轮边界与下一步

- 本轮只完成需求沉淀，没有修改任务数据模型、Excel、后台页面、课程生成器或线上版本。
- 详细需求已写入 README 的“26 天后的长期产品方向”。进入实现设计时，先完成学习模块解耦并确定通用模块协议，再确定 Excel 模板和三层数据边界（课程计划、家长临时任务、孩子每日任务），最后设计排期与发布流程。

### 补充优先级决定

- 当前第一优先级是将学习模块从页面流程和具体学科硬编码中解耦，不先扩充下一阶段课程。
- 孩子端应基于统一模块协议运行；具体课程模块通过数据配置和模块注册接入，公共的排期、展示、完成记录与奖励逻辑不随学科变化。
- 当前 26 天课程作为第一份迁移样本，解耦过程中保留已有完成记录和线上体验。

## 2026-08-07 · 学习模块基础解耦完成

### 架构调整

- 新增独立模块目录 `curriculum/learning-module-catalog.js`；默认六类学习内容、展示信息和英语课程 renderer 声明不再写在 `app.js`。
- 新增通用运行时 `learning-modules.js`。任务统一携带稳定 `id`、`moduleId`、展示元数据及可选 `activity` 描述；未知模块也能使用安全的通用任务卡展示。
- 新增 `learning-plan-presets.js`，将手部休养等区间计划的学科替换规则移出主应用；家长中心的计划模板选项由注册表生成。
- 英语课程 UI 改为按 `activity.renderer` 挂载。当天移除英语模块时互动课程同步隐藏，重新启用后恢复，不再由页面无条件展示。
- 互动进度入口改为通用 `LearningActivityProgress`，模块开始时间按孩子和 `moduleId` 保存；原 `englishExperiment` 数据自动迁移并保留兼容别名。
- 2026 年 8 月 3–6 日的历史特殊安排迁入 `legacy-learning-plan.js`，不再污染未来默认任务生成流程。
- CloudBase 发布清单已加入新的运行时、计划适配器和模块目录文件。

### 兼容与验证

- 任务原有 `id` 和完成状态保持不变；总体任务重新发布、未来日期重建及旧主站/英语岛状态合并继续按原 ID 保留完成记录。
- JavaScript 语法检查、44 项自动测试及 `git diff --check` 全部通过。
- 本地真实浏览器回归通过：默认显示 6 个模块；家长中心由注册表渲染 6 行总体任务及 2 个区间计划选项；关闭英语模块后孩子端剩 5 项且课程卡隐藏，重新启用后恢复 6 项；英语互动成功进入第 1/22 轮；控制台无警告或错误。

### 当前边界

- 本轮完成运行时解耦，没有清理短期文档、审核素材或临时目录；按用户要求将清理推迟到解耦验收之后。
- Excel 总表到模块目录/课程计划的生成器属于下一阶段；当前已建立其稳定接入边界，但尚未设计 Excel 模板。

## 2026-08-07 · 家长临时任务发布第一版

### 发布能力

- 家长中心新增“临时任务”，可填写名称、当天内容、孩子执行步骤、预计时间和适用孩子。
- 排期支持一次性、连续每天、隔一天及自选日期；发布前展示实际执行次数与日期，单次最多 62 次。
- 已发布安排可继续编辑或取消。修改只更新尚未完成的实例；取消只移除尚未完成实例，已经完成的卡片和阳光记录保留。

### 数据边界

- 新增 `family-task-schedules.js`，独立表达任务定义、排期与每日实例；记录稳定的 `taskId`、`scheduleId`、`source=parent`、`moduleId=familyTask` 和每日实例 ID。
- 通用课程任务同步补充 `taskId`、`scheduleId=default-course`、`source=course`，为后续课程版本和发布模型保留来源边界。
- 临时任务与总体任务、日期区间计划共同生成每日任务；总体任务重建、区间应用和单日恢复不会删除家长临时任务或其完成状态。
- `familyTask` 作为只由排期产生的通用模块加入模块目录，不进入每日默认六项任务；部署清单已加入新的排期运行时。

### 验证与当前边界

- JavaScript 语法检查、47 项自动测试和 `git diff --check` 全部通过。
- 本地真实浏览器验证了两人隔日任务、孩子端来源标识、编辑未完成实例、冻结已完成实例、取消保留历史以及 390px 手机布局；控制台无警告或错误。
- 本轮没有发布线上版本。下一阶段仍需统一家长中心信息架构，并建立长期课程的草稿、版本、预览和发布状态。

## 2026-08-07 · 家长中心四工作区重组

- 家长总览下新增“长期课程、临时任务、今日调整、已发布计划”四个稳定入口，一次只显示当前工作区，减少总体设置、临时发布、单日编辑和区间计划混排造成的理解成本。
- “长期课程”承接原总体任务；“临时任务”只负责创建或修改；“今日调整”合并当天任务、完成登记、错题和备注；“已发布计划”集中显示临时任务安排及日期区间计划。
- 发布临时任务后自动进入“已发布计划”；从计划列表点击编辑时自动返回“临时任务”并带入原内容。无生效计划时显示明确空状态。
- 未更改任何已有表单 ID、任务数据或同步字段，本轮只重组信息架构和导航状态。
- 47 项自动测试通过；本地真实浏览器验证四入口切换、发布/编辑跳转与空状态。390px 手机端为两列入口且无横向溢出，控制台无警告或错误。
- 本轮没有发布线上版本。下一步进入长期课程的草稿、版本、预览和发布模型。

## 2026-08-07 · 长期课程草稿与版本发布第一版

### 课程发布模型

- 新增 `course-releases.js`，按孩子保存课程草稿和不可变发布版本；版本包含课程 ID、递增版本号、生效日期、模块设置、状态及发布时间。
- 旧 `taskSettings` 保留为首次发布前及首个版本生效前的历史基线，不强制改写已有家庭数据。
- 每日任务按孩子和日期选择已经生效的最新课程版本，并在任务实例上记录发布 ID 与课程版本；两个孩子可独立发布和使用不同版本。
- 禁止把新版本插入已经排定的更晚版本之前；同一生效日期可以发布更高版本进行修订。

### 家长工作流

- “长期课程”改为“保存草稿 → 预览版本 → 发布新版本”。草稿可设置课程名称、生效日期、模块开关、展示名称和孩子步骤。
- 预览展示实际每日模块和步骤，并提示多少个已有课程完成记录的日期将保持不变。
- 发布后显示当前/未来版本状态及版本记录；草稿可放弃，不会直接影响孩子端。

### 完成记录保护与验证

- 发布只重建生效日起尚未完成的普通课程日期；过去日期、单日人工调整、日期区间计划和存在课程完成记录的日期保持原样。临时家庭任务的完成状态不会误阻止课程更新。
- 新增 3 项课程版本核心测试，完整自动测试增至 50 项；JavaScript 语法检查、部署脚本检查及 `git diff --check` 通过。
- 本地真实浏览器验证草稿状态、6 模块预览、v1 发布、生效日期与完成保护：8 月 8 日已有数学完成记录保持旧名称，8 月 9 日自动使用新名称。390px 手机端无横向溢出，控制台无警告或错误。
- 本轮没有发布线上版本。下一步是把模块目录与课程版本改为由 JSON/Excel 总表生成，并补充阶段目标、结束日期和回滚能力。

### 阶段目标与安全回退补充

- 课程草稿和发布版本新增阶段目标；目标会显示在发布预览和版本记录中，帮助区分自然拼读基础期、后续英语阶段或其他长期课程。
- 每条历史版本新增“基于此版本新建草稿”。系统复制目标和模块设置，自动选择不早于现有最新安排的生效日期；家长仍需预览并发布新版本。
- 回退不会修改、删除或重新激活旧发布记录，而是生成新的草稿和后续递增版本，保留完整审计历史。
- 新增不可变回退测试，完整自动测试增至 51 项；本地真实浏览器确认阶段目标进入预览、回退草稿带入 v1 设置、修改草稿不改变 v1 记录，控制台无警告或错误。
- 阶段结束日期暂不加入：在下一阶段课程尚未发布时直接结束整个课程版本会让非英语日常模块失去明确归属，应与课程阶段衔接模型一起实现。

## 2026-08-07 · 模块目录 JSON 数据源与发布校验

- 新增 `curriculum/learning-module-catalog.json` 作为模块目录唯一手工维护入口；六个默认模块和只由排期产生的 `familyTask` 临时任务模块已从手写 JavaScript 原样迁入。
- 新增 `scripts/generate-learning-module-catalog.mjs`，校验目录版本、模块 ID、内容、展示字段、周期与活动声明，并确定性生成浏览器使用的 `curriculum/learning-module-catalog.js`。
- 生成器支持 `--check` 只读校验；`deploy-cloudbase.sh` 在任何课程构建和上传动作之前执行该校验，JSON 无效或生成产物过期时立即停止发布。
- 自动测试直接比较 JSON 数据源与浏览器目录，并固定验证默认任务仍为原六项、`familyTask` 仍在目录中但不进入默认任务。
- 两次生成的 SHA-256 完全一致；生成校验、53 项自动测试、JavaScript/部署脚本语法检查和 `git diff --check` 全部通过。
- 下一层继续把长期课程版本改为可生成的数据，并补充课程阶段衔接、结束日期和发布回滚边界。

## 2026-08-07 · 长期课程阶段结束与无空档衔接

- 课程草稿和不可变发布版本新增可选 `stageEndDate`；课程发布协议升级为 schema v2，旧版本没有该字段时继续按原逻辑工作。
- 阶段结束日期只标记目标检查点，不直接停用每日模块。到期后若下一阶段尚未发布，当前版本继续生效并显示“等待下一阶段”；后续版本到达生效日后自动接管。
- 保存、预览和发布都会拒绝早于生效日的阶段结束日期；发布预览及版本记录明确展示阶段范围和无空档规则。
- 回退仍通过“基于历史版本新建草稿 → 预览 → 发布新版本”完成，不修改历史版本；新草稿不会继承已经过期的旧结束日期。
- 核心测试新增 schema、日期边界及跨阶段空档覆盖；完整 56 项自动测试、JavaScript/部署脚本语法、模块目录校验与 `git diff --check` 全部通过。本地浏览器验证结束日期输入、六模块预览、非法日期提示，控制台无警告或错误。未发布线上版本。

## 2026-08-07 · Excel 长期课程总表与生成校验

- 在已有 `26天英语岛课程内容总表_TTS自动入课.xlsx` 基础上生成 `../outputs/course-master-foundation-20260807/家庭长期课程总表.xlsx`，完整保留 26 天内容、音频、100 个心词和口径来源工作表。
- 新增“课程阶段、课程模块、模块字典”三张长期维护表；当前登记 `english-island-foundation-26` 相对日阶段，适用哥哥和弟弟，覆盖六个默认模块并关联 `english-course.json`。
- 新增无第三方依赖的 `scripts/generate-course-plan-catalog.py`，直接读取 xlsx 的标准 XML，确定性生成 `curriculum/course-plan-catalog.json` 并记录源工作簿 SHA-256。
- 生成校验覆盖阶段/模块引用、相对日或绝对日期范围、模块越界与重叠、临时任务模块误用，以及同一孩子 ready 绝对日期阶段冲突。
- `deploy-cloudbase.sh` 在课程构建和上传前执行 `--check`，并把生成的课程计划 JSON 加入发布包；Excel 或模块目录变化但 JSON 未重建时发布立即停止。
- 工作簿关键范围、数据验证、冻结表头和全部新增工作表已完成视觉检查；公式错误扫描为 0。下一层是让家长端从课程计划 JSON 选择 ready 阶段并生成个人草稿，仍需预览后发布。

## 2026-08-07 · 家长课程维护说明

- 新增 `COURSE_PLANNING_GUIDE.md`，明确两种维护入口：家长直接描述新课程内容，或家长先更新 Excel 总表后交由系统处理。
- 说明区分普通新阶段与真正的新模块，并固定“整理 → 生成 → 校验 → 预览 → 明确确认 → 发布”的安全流程。
- 默认不因收到内容或 Excel 改动而直接发布；历史版本、完成记录、单日调整和特殊日期计划继续受保护。

## 2026-08-07 · 暑假计划按实际完成进度顺延

### 产品规则

- 原先按自然日期自动进入下一套内容的第一期课程统一命名为“暑假计划”；26 只表示现有内容学习日数量，不再作为计划名称或连续自然日限制。
- 哥哥、弟弟分别保存当前学习日和承载该学习日任务的日期。任一项目未完成时，下一自然日继续显示原学习日；两人的进度互不阻塞。
- 当前学习日全部完成后，当天停留在完成状态，下一自然日才进入后续学习日；第 26 个内容学习日完成后不生成第 27 天。
- 家长“今日调整”新增特殊情况免除。免除项目视为已处理、可以解除顺延卡点，但保持 `done=false`，不增加阳光。
- 新完成记录写入 `completedOn`，免除记录写入 `excusedOn`，用于区分计划日期与实际处理日期；旧历史记录继续按原日期兼容。
- 双星地图对新计划按相同逻辑学习日配对，不再要求两人在同一个自然日完成；旧共同完成日仍保留。

### 架构与迁移

- 新增 `summer-plan-progress.js`，集中管理迁移、学习日推进、完成/免除判定、实际完成日和双人逻辑学习日配对；状态保存在既有家庭 JSON 的 `summerPlan` 字段，无需 Supabase 拆表或重建家庭。
- 旧家庭首次加载时以当天现有内容作为迁移起点，并根据各自英语岛开课时间推断当前学习日；不会突然回退到此前未使用的历史日期。
- 默认任务生成支持显式逻辑学习日索引。英语岛 renderer 直接读取当前计划学习日，不再用开课日期与自然日期之差决定课程内容。
- 家长总览、今日调整、联合防线和孩子首页在选择“今天”时均解析到各孩子自己的当前学习日；历史日期浏览仍保持原记录。
- 课程总表“课程阶段”标题、生成的课程计划目录和英语课程元数据均改为“暑假计划 · 英语岛基础阶段”。

### 验证

- 新增 6 项核心测试，覆盖迁移起点、未完成顺延、当天完成不立即跳转、隔日推进、免除不计阳光、第 26 天停止，以及两人在不同实际日期完成同一逻辑学习日。
- 完整 66 项自动测试、JavaScript 语法检查、模块目录校验和 `git diff --check` 全部通过。
- 本地真实浏览器确认首页显示“暑假计划第 1/26 个学习日”，英语岛显示同一学习日；普通完成为 `1/6`、10 阳光，撤销后恢复。
- 家长免除后孩子端显示“家长已免除”、进度计入 `1/6`、阳光保持 0；验证后已取消免除并恢复 `0/6`。
- 390×844 视口 `innerWidth === scrollWidth === 390`，6 张任务卡完整，无控制台警告或错误。本轮尚未发布线上版本。

## 2026-08-07 · 古诗课程恢复与数学每日填写

- 家庭课程总表新增“古诗计划”和“数学每日填写”两张表；课程阶段改为“暑假计划 · 综合学习阶段”。
- 古诗按已核对的原计划从第 11 周开始，每周依次安排学习、复习；完成第 18 周后回到第 1–5 周巩固。
- 数学不预设固定 26 日内容；家长每天通过“今日调整”填写孩子当天任务，并可在 Excel 的“数学每日填写”工作表记录实际日期、孩子、内容和检查结果。
- 新增 `scripts/generate-summer-plan-content.py`，从 Excel 确定性生成 `curriculum/summer-plan-content.json` 和浏览器 JS；发布脚本会在上传前校验产物并把两份文件加入发布包。
- `learning-modules.js` 按逻辑学习日读取古诗内容，模块目录中的古诗/乘法占位数组已移除；数学保留开放任务卡，等待家长当天填写。任务仍遵守暑假计划未完成顺延规则。
- 本轮按发布流程停在本地预览与家长确认之前，不直接发布线上版本。

### 发布后修正：古诗独立起点

- 首次发布时古诗错误地沿用了暑假计划已经推进的学习日，导致部分孩子直接显示第 14 周等后续内容。
- 古诗现在为每个孩子单独记录课程锚点：升级当天一律从第 11 周学习开始，之后仍随实际完成的学习日按“学习→复习”推进；英语和其他任务的既有进度不重置。

### 发布后修正：数学可见性与英语实际进度

- 旧总体设置或旧课程版本曾关闭数学时，升级会只对该旧版本恢复数学模块；后续新课程版本仍可重新决定模块开关。当天手填内容继续保留。
- 暑假计划学习日不再根据英语岛“开始日期经过了几天”推算，而是只依据整日任务的真实完成记录：没有完成过一个完整学习日时从 Day 1 开始；当天刚完成仍停留，下一自然日才推进。

## 2026-08-07 · 最终实现整理与旧文件清理

- 新增 `FINAL_IMPLEMENTATION.md`，集中记录当前产品形态、学习日规则、英语/古诗/数学决策、Excel 事实来源、生成链路、发布流程和必须保留的兼容层。
- README 的开发接续入口改为优先阅读最终实现文档；修正已经过期的“尚待迁移”和“尚待 Excel 接入”描述。
- 发布脚本新增 `--code-only` 模式，代码修复不再重复上传约 44 MB 的课程音频；完整课程或音频变化仍使用 `--full`。
- 删除无引用的旧 Vercel/Firecrawl 缓存、临时转写与试听目录、早期声音原型、旧重定向页、停止使用的 TTS 审核页面、浏览器 ZIP UI/依赖、已被全书目录替代的 Unit 1 映射，以及未使用的乌龙原图。
- 保留历史计划迁移、状态合并、课程版本、Oxford 审核事实、静态音频构建、Supabase schema 和历史修复 SQL，避免清理破坏已有家庭数据或可重复构建能力。
# 2026-08-10 · 家庭空间单主家庭与历史打卡恢复

- 根因确认：`get_my_families()` 末尾的 `limit 1` 让加入过多个家庭的设备在本地选择丢失后只能恢复到最早家庭；顶部“云端已同步”又没有显示家庭码，造成同步成功但空间错误的假象。
- 后端函数现返回当前匿名设备加入过的全部家庭；前端优先使用明确保存的主家庭，只有一个成员关系时自动恢复，多个成员关系时强制人工选择。
- 顶部同步状态增加当前家庭码后四位；连接面板持续显示完整邀请码，并明确说明所有设备必须显示同一码。
- “加入已有家庭”成为首选流程；“创建全新家庭”增加首设备警告和二次确认；加入、切换及已加入家庭选择都会无损合并本机记录。
- 切换状态机增加旧家庭写入隔离：等待正在执行的旧写入结束，并取消尚未执行的旧家庭 debounce，避免写入在切换后使用新家庭 ID/revision。
- 任务免除改为严格校验 `excusedOn === 任务所属日期`；因此合法的当天免除会保留，错误继承到后续日期的旧免除会被丢弃，规则适用于全部学习模块。
- 在线只读审计确认：主家庭 `67690F58` 与手机旧家庭 `BF332977` 的第 1 学习日均退化成 5/6，而历史家庭 `AB37C1F7` 仍保存两位孩子 8 月 9 日 6/6 的完整记录。
- 已从该完整证据源仅恢复主家庭两位孩子缺失的 `listening` 完成状态；更新后 `67690F58` revision 615，第 1 学习日两人均为 6/6，`completedOn=2026-08-09`。今天的任务、其他历史与旧家庭均未修改。
- 新增 `family-sync-core.js` 及 4 项家庭选择测试；状态迁移增加合法日期免除测试。完整测试 87 项通过后发布。
- 发布后复核发现只保存“设备当前选择”仍不够：设备若在新版加载前回到旧家庭，会继续保留旧 family ID。新增服务端 `family_redirects`，把 `AB37C1F7`、`BF332977` 永久指向 `67690F58`，并将旧家庭成员补充为主家庭 device 成员；旧状态不删除。`get_my_families` 只返回有效主家庭，旧邀请码加入也会落到主家庭。
