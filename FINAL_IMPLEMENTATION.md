# 双星学习岛 · 最终实现与关键决策

更新时间：2026-08-07

本文是当前实现的维护入口。历史开发过程保留在 `IMPLEMENTATION_LOG.md`，日常接续不需要从头阅读历史日志。

唯一维护目录和正式 Git 仓库为 `summer-task-dashboard`。此前用于英语岛试验的 `summer-task-dashboard-english` 已在功能稳定后合并回正式仓库；后续不再维护双份工作目录。

## 1. 当前产品形态

双星学习岛是哥哥、弟弟共同使用的家庭学习工作台。两人拥有独立的任务完成状态、暑假计划进度、阳光和花园数据；共同地图按两人完成相同逻辑学习日计算。

孩子首页固定使用统一任务协议，当前有六个默认模块：

1. 英语岛：26 个内容学习日，自然拼读、100 个心词、RAZ、口语和听力互动。
2. 写字练习：按周循环。
3. 古诗背诵：从原计划第 11 周开始，按“学习 → 复习”推进。
4. 数学：保留“今日安排”卡片，不预设固定日内容，由家长每天填写。
5. 课外阅读：按周循环。
6. 英语听力：按周循环。

家长中心负责长期课程版本、临时任务、今日调整、特殊情况免除和日期区间计划。

## 2. 最重要的进度规则

- “26”只表示当前课程内容有 26 个学习日，不代表连续 26 个自然日。
- 哥哥、弟弟各自推进，互不阻塞。
- 当天任一任务未完成，下一自然日继续显示同一个学习日。
- 全部任务完成后，当天不立即跳转；下一自然日才进入后续学习日。
- 家长免除可以解除卡点，但不增加阳光。
- 学习日只依据完整任务日的真实完成记录推算，不再依据课程开始日期或经过天数推算。
- 长期课程版本按实际使用日期生效；即使内容仍在顺延较早的学习日，也会补齐新版启用的模块，并保留同名任务的完成状态。
- 旧数据升级时保留完成、免除、阳光、单日修改、临时任务和课程版本。

## 3. 学科内容决策

### 英语岛

- 继续使用 26 个内容学习日。
- 英语互动内容来自 `curriculum/english-course.json`。
- Oxford 已审核片段与自动生成的美式英语 TTS 一起进入静态音频包。
- 英语岛与暑假计划使用同一个逻辑学习日。

### 古诗

- 固定事实来源是 Excel 的“古诗计划”工作表。
- 本阶段独立从第 11 周《峨眉山月歌》开始，不继承英语岛此前已经推进的天数。
- 每周占两个实际学习日：第一天学习，第二天复习。
- 第 11–18 周完成后回到第 1–5 周巩固，共 26 个内容学习日。

### 数学

- 不设置固定的 26 日数学课程，也不继续使用旧乘法占位内容。
- 首页始终保留“数学 · 今日安排”卡片。
- 家长在“今日调整”中填写当天内容；Excel 的“数学每日填写”用于留档。
- 旧课程设置曾关闭数学时，迁移只修复该旧版本的可见性；未来新课程版本仍可重新规划模块。

## 4. 事实来源与生成链路

主工作簿：

`../outputs/course-master-foundation-20260807/家庭长期课程总表.xlsx`

关键工作表：

- `课程阶段`：阶段名称、目标、适用孩子、相对学习日范围和状态。
- `课程模块`：阶段使用哪些稳定模块及内容来源。
- `模块字典`：模块 ID 查询。
- `古诗计划`：古诗 26 个实际学习日的固定内容。
- `数学每日填写`：数学实际安排记录，不参与固定内容生成。
- 原英语、音频、心词和来源工作表继续保留。

生成关系：

```text
Excel 总表
├─ generate-course-plan-catalog.py → course-plan-catalog.json
└─ generate-summer-plan-content.py → summer-plan-content.json / .js

learning-module-catalog.json
└─ generate-learning-module-catalog.mjs → learning-module-catalog.js

英语课程与审核事实
└─ build-english-course.mjs → english-course.json
   └─ build-static-course-audio.mjs → course-audio/
```

生成文件不直接手改；修改事实来源后重新生成并运行 `--check`。

## 5. 运行时边界

- `app.js`：页面编排、孩子/家长视图、保存、迁移与奖励，不保存具体学科课程正文。
- `learning-modules.js`：统一模块协议和默认任务生成。
- `summer-plan-progress.js`：顺延、真实完成日、免除和双人逻辑学习日。
- `course-releases.js`：不可变课程版本、生效日和阶段衔接。
- `family-task-schedules.js`：一次性、每日、隔日和自定义日期临时任务。
- `legacy-learning-plan.js`：只保留 2026-08-03 至 2026-08-06 的历史兼容，不能删除。
- `state-migration-core.js`：旧主站与英语版状态合并，不能删除。

## 6. 发布决策

- 固定流程：整理 → Excel/数据 → 生成 → 校验 → 本地预览 → 家长明确确认 → 正式发布。
- 未得到明确确认时不更新线上主入口。
- `./deploy-cloudbase.sh --main` 执行完整发布，包括重新构建和上传音频。
- `./deploy-cloudbase.sh --main --code-only` 只发布运行时代码，适合不涉及课程数据和音频的修复。
- 完整发布不删除历史版本和完成记录；迁移使用版本号幂等执行。

## 7. 当前验证基线

- JavaScript 语法检查通过。
- Excel、课程计划、暑假内容和模块目录生成校验通过。
- 自动测试覆盖模块生成、课程版本、实际进度顺延、状态迁移、英语课程和音频事实。
- 正式站已验证：未完成记录从 Day 1 开始，古诗显示第 11 周学习，数学卡片默认存在。

正式入口：<https://twin-start-island-d7dxg7911b0684-1461490716.tcloudbaseapp.com>

## 8. 明确保留的历史兼容层

以下文件看似“旧”，但仍承担数据兼容或可重复构建职责，不应随意删除：

- `legacy-learning-plan.js`
- `state-migration-core.js`
- `learning-plan-presets.js`
- `course-releases.js`
- `curriculum/week1-course.json`
- Oxford 全书目录、已审核片段与课程音频构建脚本
- Supabase schema 与历史修复 SQL
