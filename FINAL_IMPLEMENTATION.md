# 双星学习岛 · 最终实现与关键决策

更新时间：2026-08-17

本文是当前实现的维护入口。历史开发过程保留在 `IMPLEMENTATION_LOG.md`，日常接续不需要从头阅读历史日志。

唯一维护目录和正式 Git 仓库为 `summer-task-dashboard`。此前用于英语岛试验的 `summer-task-dashboard-english` 已在功能稳定后合并回正式仓库；后续不再维护双份工作目录。

## 1. 当前产品形态

双星学习岛是哥哥、弟弟共同使用的家庭学习工作台。两人拥有独立的任务完成状态、暑假计划进度、阳光和花园数据；共同地图按两人完成相同逻辑学习日计算。

孩子首页固定使用统一任务协议，当前有六个默认模块：

1. 英语岛：26 个内容学习日，自然拼读、200 个高频词、RAZ、口语和听力互动。
2. 写字练习：按周循环。
3. 古诗背诵：从原计划第 11 周开始，按“学习 → 复习”推进。
4. 数学：保留“今日安排”卡片，不预设固定日内容，由家长每天填写。
5. 课外阅读：按周循环。
6. 英语听力：按周循环。

英语岛下另有独立长期课程“语法小岛”：家长可分别为两个孩子自由选择每周学习日；语法课不计入每日任务、连续天数、地图或26日进度。每课首次完成通过独立奖励事件获得5阳光，同一孩子同一课不可重复领取。

语法小岛采用“线上讲解、口头操练与纠错检测为主，按课程标注页码选印蓝书练习为辅”的方式。规则优先用英语岛已学熟词讲清；蓝书中的少量具体名词可作为有中文提示的拓展词，但不能把不认识单词误判为不会语法。每个规则在口头操练和检测中混合多题，答错时停留并提示，订正后才继续。

家长中心负责长期课程版本、临时任务、今日调整和日期区间计划。长期任务不再提供免除：未完成就顺延，不能跳过课程内容。

## 2. 最重要的进度规则

- “26”只表示当前课程内容有 26 个学习日，不代表连续 26 个自然日。
- 哥哥、弟弟各自推进，互不阻塞。
- 当天任一长期任务未完成，下一自然日继续显示同一个学习日。
- 全部任务完成后，当天不立即跳转；下一自然日才进入后续学习日。
- 临时任务可获得阳光，但不参与长期学习日、连续天数和地图判定；到期后自然退出，不顺延。
- 学习日只依据完整任务日的真实完成记录推算，不再依据课程开始日期或经过天数推算。
- 长期课程版本按实际使用日期生效；即使内容仍在顺延较早的学习日，也会补齐新版启用的模块，并保留同名任务的完成状态。
- 旧数据升级时保留完成、免除、阳光、单日修改、临时任务和课程版本。
- 语法奖励不伪装成暑假任务；奖励事件使用稳定编号并在多设备合并时取并集，既增加可用阳光，也不改变每日任务完成率。
- 语法排课和进度进入家庭云端状态，但按孩子分别保存和合并。云端状态没有实际变化时不得重复写入；可编辑表单必须保留独立的未保存草稿，实时同步只能更新已保存状态，不能覆盖用户正在编辑的控件。

## 3. 学科内容决策

### 英语岛

- 继续使用 26 个内容学习日。
- 英语互动内容来自 `curriculum/english-course.json`。
- 高频词事实来源是 Excel 的“100个心词”兼容工作表：现包含 100 个基础心词和 100 个拓展高频词；生成器输出 `heart-word-plan.json` 后再构建英语课程。
- 20 个教学日每天学习 5 个基础词 + 5 个拓展词；拓展轮次追加在旧轮次之后，历史已完成课程日不被重新打开。
- 200 个高频词全部使用与自然拼读相同的静态 TTS 音频流水线；今日词、总词表、认读和拼写环节均可单独播放，音频生成会做响度和静音校验，播放不写入完成状态。
- 英语岛由声音实验室、核心高频词、RAZ 故事森林和高频词加餐四个独立子模块组成；孩子可按任意顺序进入，首次全部完成后才记为当日英语课程完成，已完成模块可重复练习且不重复记奖励。
- 旧线性 `completedRounds` 会按原轮次位置迁移为四模块进度；旧日已完成状态保持不变。旧版本完成日遗漏的新增拓展词在当前学习日进入一次性补学流程，先学习再拼写。
- Oxford 已审核片段与自动生成的美式英语 TTS 一起进入静态音频包。
- 英语岛与暑假计划使用同一个逻辑学习日。
- 自然拼读的 26 天规则讲解使用静态神经网络旁白配合 HTML/CSS 三段动画；动画不是视频文件，不参与完成判定。`curriculum/phonics-lesson-content.js` 是讲解示例与旁白的单一事实来源，`scripts/generate-phonics-lesson-audio.mjs` 据此生成 `phonics-media/`。
- 讲解示例与课程练习必须训练迁移而非机械记忆：任何自然拼读动画示例词都不得出现在 26 天课程的练习词集中。发布前必须校验两组词交集为空；改变示例时必须同步重新生成旁白，不能只换画面。
- 自然拼读、核心高频词、高频词加餐及动画示例均显示小号中文释义。`curriculum/english-word-meanings.js` 必须覆盖课程中的全部拼读词、200 个高频词及动画示例；拼写轮次可以把中文作为语义提示，但不能显示英文答案。
- 已完成的自然拼读课通过默认折叠的历史入口只读复习。历史动画、发音和查看行为不得写进度、奖励或重新打开已完成学习日。
- 孩子进入某个英语子模块后，顶部提示必须切换为当前模块内容；核心词、RAZ 或加餐页面不能继续显示自然拼读规则。返回模块选择时才恢复当天总主题。

### 古诗

- 固定事实来源是 Excel 的“古诗计划”工作表。
- 本阶段独立从第 11 周《峨眉山月歌》开始，不继承英语岛此前已经推进的天数。
- 每周占两个实际学习日：第一天学习，第二天复习。
- 第 11–18 周完成后回到第 1–5 周巩固，共 26 个内容学习日。

### 数学

- 不设置固定的 26 日数学课程，也不继续使用旧乘法占位内容。
- 首页始终保留“数学 · 今日安排”卡片。
- 家长在“今日调整”中填写当天内容；Excel 的“数学每日填写”用于留档。
- 旧课程设置曾关闭数学时，迁移只修复该旧版本的可见性；未来新课程版本仍可重新规划模块。

## 4. 事实来源与生成链路

主工作簿：

`../outputs/course-master-foundation-20260807/家庭长期课程总表.xlsx`

关键工作表：

- `课程阶段`：阶段名称、目标、适用孩子、相对学习日范围和状态。
- `课程模块`：阶段使用哪些稳定模块及内容来源。
- `模块字典`：模块 ID 查询。
- `古诗计划`：古诗 26 个实际学习日的固定内容。
- `数学每日填写`：数学实际安排记录，不参与固定内容生成。
- 原英语、音频、心词和来源工作表继续保留。

生成关系：

```text
Excel 总表
├─ generate-course-plan-catalog.py → course-plan-catalog.json / .js + heart-word-plan.json
└─ generate-summer-plan-content.py → summer-plan-content.json / .js

learning-module-catalog.json
└─ generate-learning-module-catalog.mjs → learning-module-catalog.js

Excel 高频词计划 + 英语课程与审核事实
└─ build-english-course.mjs → english-course.json
   └─ build-static-course-audio.mjs → course-audio/
```

生成文件不直接手改；修改事实来源后重新生成并运行 `--check`。

## 5. 运行时边界

- `app.js`：页面编排、孩子/家长视图、保存、迁移与奖励，不保存具体学科课程正文。
- `course-plan-runtime.js`：读取 Excel 生成的阶段、天数和长期模块范围。
- `learning-modules.js`：统一模块协议，按 Excel 发布阶段生成默认任务。
- `summer-plan-progress.js`：长期任务顺延、真实完成日和双人逻辑学习日；旧免除只作历史兼容。
- `course-releases.js`：不可变课程版本、生效日和阶段衔接。
- `family-task-schedules.js`：一次性、每日、隔日和自定义日期临时任务。
- `legacy-learning-plan.js`：只保留 2026-08-03 至 2026-08-06 的历史兼容，不能删除。
- `state-migration-core.js`：旧主站与英语版状态合并，不能删除。

### 媒体播放与云同步的不可变约束

`app.js` 的通用 `render()` 会在保存、云端合并、孩子/日期切换等多种路径后派发 `learning-activity-context-change`。因此任何长音频或多步骤互动模块都不能把这个事件简单理解为“必须重建页面”。

必须遵守以下规则：

1. 先计算孩子、日期、逻辑学习日组成的上下文键，再决定是否停止音频或重建 DOM。禁止在同上下文判断之前调用 `pause()`、`stopCourseAudio()` 或替换播放器节点。
2. 同一上下文中，只要存在正在进行的子模块、正在播放的媒体，或已暂停等待继续的媒体会话，就必须保留当前 DOM、`currentTime`、选中模块和按钮状态。云同步只能更新已保存数据，不能打断当前操作。
3. 切换孩子、日期或逻辑学习日属于真正的上下文变化，必须停止旧媒体并清理会话，避免声音跨孩子或跨课程继续播放。
4. 首次进入课程可能调用 `startModule()` 并同步触发全页 `render()`。必须在任何保存动作之前先写入 `activeModule`，否则核心词、RAZ、加餐可能在竞态中回落到默认的声音实验室。
5. 暂停不等于结束：暂停时保留播放器、按钮和动画 stage；再次点击调用 `play()` 继续，不得设置 `currentTime = 0`。只有播放新资源、显式返回/切换上下文或自然结束后重播，才允许归零。
6. 语法岛同样遵守会话保护：同一孩子正在学习某课时，普通云同步不得重建课程 DOM；切换孩子时才停止语法旁白和单词音频。
7. `taskSchedules` 不是整组覆盖配置，而是一组独立发布记录。所有 load、realtime、conflict 和 family-switch 合并都必须按 `schedule.id`（旧数据回退到 `taskId`）取并集，同一记录按 `updatedAt` 选择较新版本；`cancelled` 必须作为墓碑保留，防止旧设备复活已取消计划。合并恢复出云端没有的计划后，必须再次保存到云端。

这组约束来自真实故障：长讲解播放几秒后被云同步重渲染暂停；暂停后再次播放从头开始；首次启动保存期间模块选择未锁定，导致核心词、RAZ、加餐看似进入声音实验室。不得通过增加延迟、屏蔽云同步或设备特判规避，必须维护上述会话边界。

## 6. 发布决策

- 固定流程：整理 → Excel/数据 → 生成 → 校验 → 本地预览 → 家长明确确认 → 正式发布。
- 未得到明确确认时不更新线上主入口。
- `./deploy-cloudbase.sh --main` 执行完整发布，包括重新构建和上传音频。
- `./deploy-cloudbase.sh --main --code-only` 不重建或上传原 `course-audio/`，因此不会覆盖 Oxford/单词课程音频；但会携带小型课程数据、`grammar-media/` 和 `phonics-media/`，用于发布语法与自然拼读讲解更新。
- 完整发布不删除历史版本和完成记录；迁移使用版本号幂等执行。

## 7. 当前验证基线

- JavaScript 语法检查通过。
- Excel、课程计划、暑假内容和模块目录生成校验通过。
- 自动测试覆盖模块生成、课程版本、实际进度顺延、状态迁移、英语课程和音频事实。
- 媒体或互动入口改动不能仅靠源码断言或测试数量验收。发布前必须在真实浏览器逐条操作并记录结果：四个英语模块分别进入正确内容；播放中触发同上下文刷新仍继续；暂停后时间不增长；暂停期间刷新仍保留位置；继续后 `currentTime` 从原位置增长；切换孩子会停止旧媒体；返回模块选择后可再次进入；中文释义可见；动画示例与练习词的实际 DOM 交集为空。
- 2026-08-17 的真实回归基线：普通暂停保持在 4.29 秒并从该位置继续；同孩子刷新期间保持在 3.40 秒并继续增长；声音实验室、核心词、RAZ、加餐分别显示对应状态与首题；讲解 `bat/jam/van/pack/tack` 与练习 `ant/cat/man/pan/back/sack` 无交集。
- 正式站已验证：未完成记录从 Day 1 开始，古诗显示第 11 周学习，数学卡片默认存在。

正式入口：<https://twin-start-island-d7dxg7911b0684-1461490716.tcloudbaseapp.com>

## 8. 明确保留的历史兼容层

以下文件看似“旧”，但仍承担数据兼容或可重复构建职责，不应随意删除：

- `legacy-learning-plan.js`
- `state-migration-core.js`
- `learning-plan-presets.js`
- `course-releases.js`
- `curriculum/week1-course.json`
- Oxford 全书目录、已审核片段与课程音频构建脚本
- Supabase schema 与历史修复 SQL
